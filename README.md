# 糖心志者

糖心志者是一个 Chrome Manifest V3 浏览器插件，用可爱的「糖果手帐」工作台和独立浏览器标签页中的「沉浸糖心影院」承载安全账号池、智能播放、视频下载、服务体检、版本更新和缓存整理能力。

## 项目介绍

界面源码位于 `ui-src/`，通过 Vite 打包为 `dist-ui/txzz-ui.js` 和 `dist-ui/txzz-ui.css`。同一份生产资源同时服务目标站 Shadow DOM 工作台和 `cinema.html` 独立影院页，二者不会再因两套预览/构建入口产生视觉漂移。`content.js` 继续承担网站页面集成；独立影院通过 `CinemaStandaloneApp` 直接使用 Service Worker 状态桥。React 端已经拆为领域规则、跨页视图模型、偏好 Hook、工作台壳层、页面组件和设计令牌，页面不再自行重复解释后台状态。

插件默认只显示右下角「糖糖」伙伴入口，不再向网站左上角注入流程气泡或当前卡片标签。点击伙伴后展开糖果手帐工作台：桌面端使用浅色手帐侧栏与独立工作区，移动端使用全屏面板和安全区底栏；面板只保留今日总览、照料中心、账号小屋和影院入口，所有浏览、播放、合集、书签、下载与存储能力都收口到独立「糖心影院」标签页。

当前源码版本为 `5.7.0`（构建 `2026-07-31-0145`）；正式 CRX 发布时继续使用 4.0.0 已启用并妥善保留的私钥，扩展 ID 固定为 `ddefadnhgebdclpkabeobjidjllkdkhm`，因此 4.0.0 及 5.x 用户可原位覆盖升级。更早的旧 ID `ghbbddahmhhmjknofkmdkcflbmplcace` 因原私钥永久丢失，仍需先移除旧扩展再安装当前版本。

## 环境要求

- 操作系统：Windows 10 或 Windows 11
- 浏览器：Chrome `120.0` 及以上，或 Edge `120.0` 及以上
- 插件规范：Chrome Manifest V3
- 本地检查工具：Node.js `22.16.0` 及以上
- 本机验证版本：Node.js `25.8.0`，npm `11.11.0`
- 前端界面：React `18.3.1`，React DOM `18.3.1`，lucide-react `0.487.0`
- 播放器内核：Shaka Player `5.2.4`（统一负责 HLS、AES-128、ABR、错误恢复、清晰度与 TS 转封装）
- 播放器界面：糖果影院自有 React 控制层，交互状态与媒体内核分离；不再依赖第三方播放器 UI
- 目标帧预览：标准 HLS 缩略图轨优先；目标站缺少缩略图时使用隔离的 360P/900 Kbps Shaka 预览会话，主播放器在松手前不跳转
- 构建工具：Vite `8.1.4`，@vitejs/plugin-react `6.0.3`，Tailwind CSS `4.3.2`，@tailwindcss/vite `4.3.2`，TypeScript `7.0.2`
- 测试工具：Vitest `3.2.7`；发布前固定运行全部纯核心与打包器回归测试
- CRX3 打包：仓库内置确定性 ZIP + RSA-SHA256 打包器，只使用 Node.js `crypto` / `zlib`，不再依赖 `crx`、`archiver` 或旧版 `glob`
- 依赖自检：`scripts/check-deps.mjs` 会在构建前检查 Vite、Shaka Player 主入口和 TS 转封装 Worker
- 远程服务：Cloudflare Worker
- 云端数据库：Supabase
- 部署工具：Wrangler `4.110.0`
- 视频转封装：`mux.js@7.0.0`
- HLS 下载清单解析：`m3u8-parser@7.2.0`
- 大文件持久化：浏览器 OPFS；不支持 OPFS 的浏览器不能使用可恢复下载与安全保存页
- 文件编码：UTF-8

## 核心功能

- 伙伴入口：默认只显示右下角「糖糖」CSS 伙伴，不显示任何左上角流程标签或悬浮 HUD；支持桌面端和移动端拖动，位置会限制在可视区域并持久保存。
- 糖果手帐工作台：莓果粉、薰衣草、薄荷和杏子色只承担动作与状态，暖白底负责长时间阅读；桌面端使用浅色手帐侧栏，移动端转为全屏工作区和安全区底栏。
- 业务分层：`domain/` 管理账号与下载规则，`model/` 统一导航与跨页状态推导，`cinema/` 管理目录白名单与查询契约，`playback/` 管理播放会话 reducer、媒体内核、线路策略、续播和全屏控制，`hooks/` 管理通用偏好，`components/layout/` 负责壳层；页面只消费统一 ViewModel。
- 四个面板空间：导航只包含今日总览、照料中心、账号小屋和影院入口；播放、片库、书签、足迹、下载与 OPFS 存储均在独立影院中完成，避免同一业务分散到两套页面。
- 今日总览：按「今日运行清单 → 正在进行 → 快捷动作 → 小日记」组织内容，优先告诉用户当前风险与下一步，而不是只罗列统计数字。
- 页面状态展示：集中展示会员状态、扩展运行状态、账号池状态和最近流程。
- 糖心影院：运行在独立的 `chrome-extension://…/cinema.html` 标签页，提供首页、发现、搜索、片库、足迹、下载、详情与播放路由；重复点击入口会通过扩展上下文识别并复用同一标签，避免重复播放器。影院使用目标站 `/movie/block`、`/movie/search` 和用户打开详情后的只读合集元数据构建分区、分页海报墙与选集。目录对象按字段白名单归一化，广告区块、无编号影片和播放 URL 均被丢弃；查询页以 5 分钟 TTL 保留在 `chrome.storage.session`，可跨 Service Worker 重启恢复。目标站 `.bnc` 海报只在进入视口后由后台按 AES-128-ECB/PKCS7 解密，并校验当前目录或独立片库归属、HTTPS、大小和图片魔数。只有用户明确点击开映或下载后，才获取完整线路；详情页、合集当前集与播放器均可进入同一下载规划器，完整边界见 [`docs/cinema-system.md`](docs/cinema-system.md)。
- 播放状态展示：独立影院宽屏采用「播放器主舞台 + 本场影片侧栏」，片源、下载、书签和足迹收纳在放映抽屉；窄屏自动改为单栏。解析、账号轮换和安全解锁使用可见步骤条，首次资源就绪后仍保持暂停，必须由用户点击「开映」。合集可直接在视频画面选集；免费/VIP 下一集结束后 5 秒续播，金币分集必须再次确认。画面、侧栏和抽屉均复用同一 OPFS 规划、调度与保存链路。Vlog 当前卡片继续在后台交叉核对活动 DOM、播放器 ID、Vue `isActive` 与 Swiper 索引，但不再向网站注入电影票 HUD；换片时保留最后一个稳定 ID，并拒绝旧请求写回新卡片。
- 账号池管理：账号池页重构为控制台布局，支持账号搜索、本地可靠编辑、删除二次确认、云端自动轮换和云端优先本地兜底。
- 云端轮换策略：云端账号按金币数量升序排序；获取播放详情失败或金币视频处理失败时自动切换下一个云端账号。
- 金币视频策略：先检查主线路和备用线路；VIP 等账号已经返回可播放链接时立即使用，绝不调用购买接口。无扩展名签名地址、相对播放地址同样按有效线路处理；只有确实没有链接且仍为金币锁定时，才从金币最少账号组中随机选择一个账号购买。
- 云端账号卡片：统一展示账号昵称、可用状态、不可用原因、普通 VIP、尤物圈和金币余额；云端账号不提供手动选择按钮，只参与自动轮换；失效云端账号默认隐藏，可手动查看。
- 云端分类识别：前端账号池页面和后台统一识别 `cloudReadonly`、`isCloud`、`remoteId`、`cloudId`、`source=remote`、`source=qrcode` 等云端摘要标记，避免云端账号误显示到本地账号分类。
- 账号可用判定：账号池会区分账号凭据错误和视频资源异常，视频下架、播放详情缺少链接或临时接口异常不会把有成功检查记录的云端账号误判为失效。
- 关键流程反馈：同步账号池、账号轮换、金币安全核对、下载和异常只在工作台内部展示；网站左上角不再出现流程标签，被动网络观察仅写入诊断记录。
- 添加账号面板：通过「添加账号」按钮展开，支持账号密码、账号凭证、token/deviceId 三种本地保存方式。
- 远程同步：通过 Cloudflare Worker 读取 Supabase 账号池，扩展后台自动携带与 Worker 配套的内置 Bearer 访问密钥；用户只需填写服务地址，页面环境只看到脱敏摘要。
- 云端服务体检：设置页通过扩展后台检测 `/v1/diagnostics`、`/v1/status` 和 `/v2/health`；v2 健康检查同时验证播放安全迁移是否就绪，再次打开时会按 Worker 地址展示匹配的上次体检结果。
- 体检快捷处理：体检结果下方提供「处理账号池」「同步账号」「复制报告」三个快捷操作，可直接跳转账号池、打开失效账号视图或复制完整体检报告。
- 播放页面：扩展只调用 Worker `POST /v2/playback/session`，把服务端线路决策、账号与获取摘要转成单一 `screening` 状态。页面编排与媒体生命周期完全分离：单 reducer 管理 `idle → resolving → ready → loading → playing/paused → buffering/switching → ended/error`，每个异步事件携带会话代次；主链路任意时刻只保留一个 Shaka Player 和一个原生 video，后发装载会使旧 `attach/load` 任务失效。同一会话 URL 指纹变化时保留进度与暂停状态后重载，同 URL 只刷新健康元数据。9:16 视频使用受动态视口约束的黑色影院画布；设置菜单固定为片源、观看、工具三类。标准/镜像双击与长按可持久化，横向滑动始终左退右进；横滑和直接拖动进度条使用独立低码率目标帧预览，主画面在手势期间固定于起点，松手后只正式跳转一次。线路错误和恢复次数按线路隔离，起播超过 8 秒、30 秒内累计 3 次 Shaka 致命错误或恢复失败时只自动切一次尚未尝试的备用线路。续播按视频编号每 5 秒保存，15 秒以内、距结尾不足 30 秒或超过 30 天的记录不会恢复。完整功能矩阵见 [`docs/player-system.md`](docs/player-system.md)。
- 下载管理：下载前先选择线路、播放模式、清晰度和容器，并展示预计大小、耗时、可用空间及兼容性。任务以 `attemptId + sequence` 隔离旧消息，分片和成品写入 OPFS，支持暂停、继续、取消、失败重试与浏览器重启恢复；删除或清空会先中止请求再清理 OPFS。清单上限 2 MiB、单分片 128 MiB、单任务默认上限 8 GiB，请求连续 20 秒无数据即超时，分片最多重试 3 次。
- MP4 输出：锁定 `m3u8-parser@7.2.0` 解析 AES-128 序列 IV、显式 IV、初始化段、Byte Range、密钥轮换和 discontinuity；TS 分片优先流式转封装为 MP4。直播、SAMPLE-AES 和无法安全合并的独立音轨会在规划阶段明确拒绝，不生成损坏或静音文件。
- 购买安全对账：设置页显示云端与本地 `pending / charged / uncertain` 记录；对账只使用原购买账号重新获取详情，绝不调用 `doBuy`，成功后进入 `resolved`，失败继续保持安全阻断且只展示脱敏错误。
- 数据清理：设置页通过明确的「完整重置」操作清除旧版本缓存、账号池缓存、播放缓存、下载缓存、更新缓存和页面运行缓存，不再使用容易误解的假复选框。
- 设置分类：云端服务、体验功能、版本升级和数据管理使用分类导航，低频与高风险操作不再挤在同一长页面。
- 旧数据迁移：首次升级到 5.0.0 时会把旧 `fullDetails` 转换为新的放映历史；账号池、下载记录和设置保持不变。购买防重复账本与 `screening` 使用独立 schema，旧格式只迁移一次，不会覆盖新会话。
- 更新管理（升级系统 v8）：后台并发读取固定仓库路径下的多份 `update.json`，先用内置正式公钥验证完整清单签名，再按 **version + build + 来源优先级**选取最新。安装包通过大小、SHA-256、CRX3 结构、正式扩展 ID 和包自身 RSA 签名校验后写入 OPFS，再打开扩展来源的安全保存页；保存页再次校验大小、CRX3 文件头和 SHA-256，并通过一次性令牌限制单标签领取。整个 CRX 不再通过 runtime 响应传输 Base64，也不依赖 `chrome.downloads.download`。完整信任链见 [`docs/update-system.md`](docs/update-system.md)。
- 响应式适配：手机视图使用安全区底部导航和独立内容滚动，桌面视图使用侧栏工作区；插件采用独立尺寸基准，不受宿主网站根字号缩放影响。最终版本在真实网站以 `320`、`390`、`640`、`1024`、`1536` 像素宽度检查布局，并支持系统减少动态效果设置。

## 项目目录结构

```text
tangxin-zhizhe-extension/
├── manifest.json              # Chrome MV3 插件清单
├── cinema.html                # 独立浏览器标签页中的糖心影院入口
├── cinema.css                 # 独立页 light DOM、Shadow Host 与首屏背景基线
├── package.json               # React 界面、Shaka Player 媒体内核和构建依赖，所有版本已固定
├── package-lock.json          # npm 锁定文件，保证依赖安装结果一致
├── vite.config.ts             # Vite 打包配置，输出扩展可加载的 dist-ui 资源
├── tsconfig.json              # React TypeScript 源码检查配置
├── scripts/
│   ├── check-deps.mjs         # 构建前依赖完整性自检和 node_modules 损坏自动修复脚本
│   ├── check-release.mjs      # 深度发布门禁：清单签名、CRX3 签名、ZIP/CRC32 和运行时文件逐字节核对
│   ├── crx3-pack.mjs          # 可审计的确定性 ZIP + CRX3 RSA 打包器，无第三方归档依赖
│   ├── release-config.mjs     # 打包与门禁共用的正式 ID、公开公钥、镜像和运行时文件清单
│   └── pack-crx.mjs           # 固定私钥打包 CRX3，计算哈希并签署外置 update.json
├── docs/
│   ├── cinema-system.md       # 影院目录、只读边界、查询缓存与按需开映链路
│   ├── player-system.md       # 播放器功能矩阵、模块边界、全屏状态与验收清单
│   └── update-system.md       # 升级系统 v8 状态矩阵、签名信任链、OPFS 保存与安装边界
├── releases/                  # 对外分发的 CRX 安装包（latest + 版本号）
│   ├── tangxin-zhizhe-latest.crx
│   ├── tangxin-zhizhe-x.y.z.crx
│   ├── latest.json            # 当前版本、构建、文件名、大小、sha256 与固定扩展 ID
│   ├── extension-id.txt       # 固定签名密钥对应的扩展 ID
│   └── signing-public-key.json # 可公开的更新验证公钥、指纹和正式扩展 ID
├── keys/                      # 本地签名密钥（.pem 不入库，务必备份）
├── ui-src/                    # React 糖果手帐工作台源码
│   ├── main.tsx               # 双入口挂载：目标站 Shadow DOM 工作台与独立影院页
│   ├── app/                   # 状态桥、领域规则、视图模型、工作台壳层与六个业务页面
│   │   ├── CinemaStandaloneApp.tsx # 独立影院 Service Worker 状态桥与页面编排
│   │   ├── domain/            # 账号凭据校验、下载筛选排序等纯业务规则
│   │   ├── hooks/             # 页面偏好和悬浮伙伴位置持久化
│   │   ├── model/             # 导航单一事实来源与跨页工作台视图模型
│   │   ├── cinema/            # 影院目录、独立页桥、Hash 路由与纯核心测试
│   │   ├── playback/          # 会话 reducer、线路策略、媒体内核、全屏、偏好、续播与迁移测试
│   │   ├── download/          # 下载规划、清单能力判断与固定场景测试
│   │   ├── update/            # 升级系统前端：状态机、共享进度、升级中心和更新弹层
│   │   └── components/
│   │       ├── ui/            # 面板统一设计系统（SectionCard、StatGrid、按钮、可访问弹层等）
│   │       ├── layout/        # CSS 伙伴、悬浮入口、桌面侧栏、移动底栏和工作台外壳
│   │       ├── cinema/        # 影院 Hero、筛选、海报卡片与影片详情弹层
│   │       ├── playback/      # 播放领域计算与报告生成
│   │       ├── player/        # 播放器主控栏、状态覆盖层、手势和全屏适配
│   │       └── screening/     # 糖果影院主舞台、影片侧栏和片源/下载/足迹抽屉
│   └── styles/                # Tailwind v4 入口样式与「糖果手帐」设计令牌
├── dist-ui/                   # Vite 构建产物，扩展运行时直接加载
│   ├── txzz-ui.js             # 糖果风 React 控制台脚本
│   ├── txzz-ui.css            # 糖果风 React 控制台样式
│   └── shaka-player.transmuxer-worker.js # MV3 CSP 允许的本地 TS 转封装 Worker
├── background.js              # 后台服务，负责账号池、远程请求、播放详情和下载任务
├── state_mutation_core.js     # 浏览器状态三方合并与串行 mutation 纯核心
├── cinema_catalog_core.js     # 目录字段白名单、广告过滤、搜索参数和分页去重纯核心
├── cinema_poster_core.js      # 加密海报 URL 校验、AES-ECB 解密、PKCS7 与图片魔数纯核心
├── page_context_core.js       # Vlog 当前卡片交叉判定与页面代次纯核心
├── download_core.js           # M3U8 规划、限制、AES/Map/Byte Range 规则纯核心
├── content.js                 # 页面脚本，负责业务能力、页面监听和 React 状态事件桥
├── display_patch.js           # 页面展示覆盖逻辑
├── nav_guard.js               # 页面导航守卫和刷新控制逻辑
├── page_hook.js               # 页面主环境请求监听和播放资源处理
├── page_probe.js              # 页面会话和用户状态探针
├── offscreen.html             # 离屏下载页面
├── offscreen_downloader.js    # OPFS 分片、恢复、组装、转封装和 CRX 暂存
├── save.html                  # 扩展来源的一次性安全保存页
├── save.css                   # 安全保存页糖果主题样式
├── save.js                    # OPFS 读取、CRX 复核、文件写盘与票据核销
├── update.json                # 外置 schema 3 签名更新清单（不打进 CRX，避免哈希自引用）
├── vendor/
│   ├── mux-7.0.0.min.js       # 固定版本 mux.js，用于 TS 转 MP4
│   └── m3u8-parser-7.2.0.min.js # 固定版本 HLS 清单解析器
└── README.md                  # 当前插件文档
```

## 安装教程

### 方式一：安装 CRX（推荐，更新也下这个）

1. 下载最新安装包：  
   `https://github.com/lsy5920/tangxin-zhizhe-extension/raw/main/releases/tangxin-zhizhe-latest.crx`
2. **Kiwi 浏览器（安卓）**：下载后点开 `.crx` 即可安装/更新。
3. **Chrome / Edge 桌面端**：
   - 打开 `chrome://extensions/`（或 `edge://extensions/`）
   - 打开右上角「开发者模式」
   - 把 `.crx` 文件拖进扩展页面完成安装  
   - 若浏览器拦截「非应用商店扩展」，请用下方「方式二」加载解压目录，或允许来自其他来源的扩展（视浏览器策略而定）
4. 以后在插件「设置 → 升级中心」点「下载」也会直接下载同款 `.crx` 文件。

### 方式二：开发者模式加载源码目录

1. 打开 Chrome 或 Edge。
2. 在地址栏输入 `chrome://extensions/` 并回车。
3. 打开右上角「开发者模式」。
4. 点击「加载已解压的扩展程序」。
5. 选择当前目录：`tangxin-zhizhe-extension`。
6. 如果刚修改过界面源码，先在当前目录执行 `npm ci`，再执行 `npm run check` 生成并验证 `dist-ui/`。
7. 初次只使用已构建好的插件时，可以直接加载当前目录；如浏览器提示缺少 `dist-ui/txzz-ui.js`，说明需要先执行上一步构建命令。
8. 打开需要联调的页面。
9. 页面右下角出现「志」字悬浮球，表示插件加载成功。

## 远程账号池配置

推荐使用远程账号池保存账号凭据。账号密码、二维码凭证、token、deviceId、Supabase `service_role` 等敏感信息应放在 Cloudflare Worker 或 GitHub Secrets 中。插件后台只保存 Worker 地址；服务访问密钥已与对应 Worker 一起内置，用户无需填写，React 页面不会获得密钥明文。

配置步骤：

1. 点击「志」字悬浮球打开面板。
2. 进入「账号池」页。
3. 在「云端连接」中填写「云端服务地址」。
4. 选择账号来源模式。
5. 点击「保存并验证」，插件会自动使用内置访问密钥。
6. 点击「同步云端」读取云端账号池。
7. 云端账号默认只显示可用账号；需要排查坏账号时，打开「查看失效」。

账号来源模式说明：

| 模式 | 说明 |
| --- | --- |
| 云端轮班 | Worker 按金币数量从少到多选择可用账号，失败时自动切换下一个账号。 |
| 本地值班 | 只使用插件本地保存并选中的账号。 |
| 云端优先，本地兜底 | 优先使用云端账号，失败后尝试本地账号。 |

## 页面说明

| 页面 | 用途 |
| --- | --- |
| 今日总览 | 查看运行清单、账号与下载摘要、正在进行、小日记和常用操作。 |
| 糖心影院 | 打开独立浏览器标签页，以完整影视 App 布局浏览、搜索、筛选、收藏和回看目标站片单；支持合集选集、画面内换集、自动续播与按需下载，目录阶段不取完整线路。 |
| 账号小屋 | 管理 Worker 地址、本地账号、云端轮班、上传云端和失效账号查看。 |
| 放映室 | 使用沉浸糖果影院播放当前影片，并在侧栏查看检票步骤、线路决策、获取方式和账号摘要，在抽屉管理片源、下载和足迹。 |
| 下载收纳篮 | 管理下载任务、保存到设备、删除任务、打开下载目录和清空任务。 |
| 照料中心 | 按云端体检、体验魔法、版本更新和数据收纳处理诊断、更新、缓存与重置。 |

## 使用教程

### 打开和关闭面板

1. 打开目标页面。
2. 点击右下角「糖糖」伙伴入口。
3. 伙伴入口隐藏，工作台展开，默认进入上次使用的页面。
4. 点击关闭按钮后，工作台收起，伙伴入口重新出现。
5. 按住伙伴入口拖动可调整位置；位置会自动限制在屏幕内并保存，窗口变化时也会重新校正。
6. 点击主界面右上角「关于」按钮，可打开项目主页。
7. 桌面端通过左侧导航切换页面，手机端通过底部导航切换页面。
8. 进入网站视频详情页时，插件会阻止网页视频自动播放并默认暂停；点击播放器区域或播放按钮后可正常播放。

### 打开独立糖心影院

1. 在目标站点击右下角「糖糖」打开工作台，再点击「糖心影院」。
2. 浏览器会打开 `cinema.html` 独立标签页；首页、发现、搜索、片库、足迹和下载使用影院自己的侧栏或移动底栏，不嵌在网站工作台内部。
3. 已经打开影院时再次点击入口会聚焦并复用原标签，不会创建第二个播放器实例；浏览器恢复出的影院标签同样可以被识别。
4. 首页和发现页只抓取原始目录。打开影片详情后才读取白名单化的合集分集，响应中的播放字段不会进入影院状态。
5. 点击「获取完整线路并开映」或「下载」后才进入可见检票、账号轮换、线路决策与下载规划；资源就绪后仍默认暂停。
6. 桌面使用可折叠影院侧栏，移动端使用六项安全区底栏；浏览器返回键可在详情、播放与主导航历史之间返回。

### 应用页面状态展示

1. 打开插件面板。
2. 点击「应用展示覆盖」。
3. 面板会刷新当前状态区域。
4. 如果页面内有对应状态展示，插件会同步进行页面侧显示更新。

### 查看播放状态

1. 打开任意视频详情页或播放页。
2. 插件会自动记录最近视频和播放处理状态。
3. 进入「放映室」查看播放器主舞台、本场影片侧栏，以及片源、下载、片库、书签、足迹五个抽屉页签。
4. 侧栏步骤条会显示影片识别、账号轮换、跳过或执行金币解锁、送达放映线路等真实阶段；有直链时购买阶段明确标记为跳过。
5. 资源就绪后默认暂停，点击中央「开映」或控制栏播放按钮才会开始；空格/K 同样可播放或暂停。
6. 点击「重新检票」会使用新的 `requestId` 请求 Worker v2 会话，重新评估缓存、账号、线路和获取方式，不会创建下载任务。
7. 播放器画面右上角、影片侧栏和片源/下载抽屉都可打开同一个下载规划器；完整链接复制与打开保留在片源抽屉，足迹按视频会话展示迁移后的历史。
8. 播放器使用 Shaka Player `5.2.4`；线路卡和播放器设置均可手动换线，自动切线只会选择本场尚未自动尝试过的线路。
9. 在插件播放页点击全屏时，会优先请求浏览器全屏；如果浏览器只能让插件宿主全屏，会自动进入播放全屏模式，隐藏面板外壳、侧栏、标题栏、底部导航和其它播放页卡片，播放器外层会同步无框覆盖整个视口，视频内容默认按原比例尽可能完整显示；如果浏览器拒绝真实全屏，会自动使用插件内沉浸全屏兜底。
10. 播放器进入全屏后，顶部会显示全屏来源、视口覆盖率和容器贴合体检；如果出现圆角、内层视频框、裁剪或宿主全屏模式异常，可点击「诊断」在页内查看报告，报告会包含播放器容器尺寸、当前视口尺寸、覆盖率和异常原因。
11. 播放器内置进度条可点击跳转定位；视频画面默认只显示播放/暂停、后退、前进、菜单和全屏主控按钮，全屏时会额外显示「锁定」按钮。锁定后停止全部画面手势，只保留右下角解锁按钮；播放稳定后控制层自动隐藏。
12. 点击「菜单」按「片源」「观看」「工具」三类设置：片源负责播放策略、线路和清晰度；观看负责标准/镜像手势、倍速、跳转步长、画面、亮度和音量；工具负责重载、画中画、截图、全屏校准和诊断。复制与打开统一在片源抽屉，下载保留画面快捷入口、侧栏主动作和下载抽屉，静音统一在主控制栏。
13. 播放器支持键盘热键、截图、画中画、浏览器全屏、系统媒体键、双击、长按和移动端手势。标准手势是左退右进，镜像手势是左进右退；横向滑动方向始终保持左划回退、右划前进，并悬浮显示调整后目标时间的实时视频小画面。空格或 K 播放暂停，方向键跳转/调音量，M 静音，F 全屏；上次未看完的视频会自动恢复到上次进度。
14. 如果播放器卡住或换线后没有刷新，在「菜单」的「工具」里点击「重载」重新载入当前播放源。
15. 如果主线路播放不稳，在「菜单 → 片源」直接选择其他健康线路；播放异常时插件也会自动尝试尚未使用的备用线路。
16. 如果觉得快进快退幅度不合适，在「菜单 → 观看」选择 5 秒、10 秒、30 秒或 60 秒。
17. 如果全屏后两侧有黑边，这是为了保持原比例完整显示；在「菜单 → 观看」切换「原比例」「裁满」「铺满」，其中「裁满」可能裁切边缘，「铺满」可能轻微变形。
18. 如果网页视频偏暗或偏亮，在「菜单 → 观看」拖动亮度滑块，可在 60% 到 140% 之间调整。
19. 点击完整播放器右键菜单里的「复制完整链接」「打开完整链接」「查看播放器诊断」，可快速排查当前播放源和全屏容器状态。
20. 点击完整播放器外层或「菜单」的「工具」里的「复制」会复制当前播放源完整链接，点击「打开」会用补全域名后的完整链接在新窗口打开当前播放源。
21. 点击「打开线路」或主备线路卡片里的「打开」，也会用补全域名后的完整链接在新窗口打开当前线路，方便直接验证主线路或备用线路是否能播放。
22. 合集影片会在画面右上角显示「选集 X/Y」；切集会重新进行可见检票，不复用上一集签名地址。自然播放结束后，免费/VIP 下一集倒计时 5 秒续播，金币下一集必须再次确认。
23. 如果当前视频支持下载，可从影院详情、合集当前集、播放器画面、侧栏或下载抽屉点击下载；探测中立即显示进度，失败会保留错误和重新探测入口。
24. 创建下载任务后，播放页会显示当前视频的下载阶段、分片进度、文件大小、更新时间和完整源链接摘要。
25. 当前视频下载卡片可直接复制完整下载源链接；如果任务失败，可直接点击「重试下载」重新创建任务。
26. 足迹抽屉可把任意历史会话载入主舞台预览；该会话始终使用自己的视频编号、线路和决策，不会串用当前影片数据。
27. 下载合并完成后，可在放映室或收纳篮保存到设备；失败任务仍可在收纳篮重试和查看报告。

### 管理账号池

1. 进入「账号池」页。
2. 顶部统计区会显示全部账号、云端可用账号、本地账号和失效账号数量。
3. 云端账号卡片统一显示账号昵称、可用状态、不可用原因、普通 VIP、尤物圈和金币余额。
4. 失效云端账号默认隐藏；打开「查看已失效云端账号」后可以查看不可用账号和失败原因。
5. 搜索框可按账号名称、用户名、编号或备注同时筛选云端和本地账号。
6. 点击「添加账号」会先弹出类型选择窗口。
7. 选择「账号密码」后，只显示账号昵称、用户名和密码等必填内容。
8. 选择「账号凭证」后，只显示账号昵称和账号凭证内容。
9. 选择「token/deviceId」后，只显示账号昵称、deviceId 和 userToken。
10. 点击「保存本地」只保存到插件本地账号池。
11. 点击「保存并上传云端」会把账号提交给 Worker 加密保存。
12. 上传成功后，同步回来的云端卡片会继续显示账号昵称、凭据类型、可用状态、不可用原因、普通 VIP、尤物圈和金币余额。
13. 本地账号卡片可选择、编辑、检查、上传云端或删除；编辑时凭据留空会保留后台原值，删除前必须二次确认。云端账号只显示摘要，不能手动固定或修改凭证明文。

### 下载视频

1. 打开目标站视频详情，或进入「糖心影院」选择影片/合集分集。
2. 点击影院详情、播放器画面、影片侧栏、片源抽屉或网站页面上的下载按钮。
3. 在下载规划器选择线路、省流/均衡/高清模式、清晰度和容器；确认预计大小、耗时、可用空间与兼容性。可用空间已知且不足预计大小 115% 时不会启动。
4. 插件创建唯一 `attemptId` 后把任务交给离屏下载器；MP4 和 HLS 都写入 OPFS，不再把大视频完整保存在内存。
5. M3U8 会按清单下载并校验分片，再流式组装或转封装；不支持的直播、SAMPLE-AES、独立音轨会在开始前说明原因。
6. 进入「下载」页查看进度；任务可暂停、继续或取消，浏览器/离屏页重启后会按检查点恢复。
7. 任务较多时，可点击「全部」「进行中」「可保存」「失败」快速筛选。
8. 可在搜索框输入标题、视频编号、任务编号、阶段、失败原因或链接，进一步缩小当前筛选结果。
9. 可切换「最近」「失败优先」「进度」「大小」排序，排序会影响当前可见列表和复制报告顺序。
10. 如果当前范围里有失败任务，页面会显示失败原因概览，按原因聚合数量，方便先处理高频问题。
11. 点击「失败摘要」会复制当前范围里的失败原因、视频编号和完整源链接。
12. 下载任务卡片会显示完整源链接摘要，点击复制按钮会复制包含 `https://域名/...` 的完整下载源链接。
13. 点击「复制筛选链接」会批量复制当前筛选、搜索和排序结果中的完整下载源链接，每行一个链接。
14. 点击「复制报告」会复制当前筛选、搜索和排序后的任务状态、格式、进度、文件大小、更新时间、失败原因和完整源链接。
15. 如果任务失败，可在失败任务卡片点击「重试」重新创建同一视频的下载任务。
16. 切换到「失败」筛选后，可点击「重试失败」按视频编号去重批量重新创建当前结果中的失败任务。
17. 切换到「可保存」筛选后，可点击「保存全部」逐个处理当前结果里的待保存任务。
18. 合并完成后点击「保存到设备」，在扩展安全保存页核对文件并由用户手势启动保存。支持文件选择器时写盘成功后自动完成；Android Blob 兜底会保留票据，看到下载文件后再手动确认。

### 管理下载任务

「下载」页支持：

- 查看总任务、进行中、已完成和失败数量。
- 按全部、进行中、可保存和失败快速筛选下载任务。
- 按标题、视频编号、任务编号、阶段、失败原因和链接搜索下载任务。
- 按最近更新、失败优先、进度最高和文件大小重排当前结果，方便优先处理失败、接近完成或大文件任务。
- 按当前可见任务聚合失败原因，并可复制失败摘要用于排查。
- 一键保存当前筛选和搜索结果中的全部可保存任务，已保存任务不再占用可保存数量。
- 批量复制当前筛选、搜索和排序结果中的完整下载源链接。
- 复制当前筛选、搜索和排序任务报告，用于排查失败原因、核对分片进度和保存完整源链接。
- 失败任务可直接点击「重试」重新创建同一视频的下载任务。
- 失败筛选下可点击「重试失败」批量恢复失败任务。
- 查看视频名称、视频编号、输出格式、文件大小和更新时间。
- 查看完整源链接摘要，并复制包含域名的完整下载源链接。
- 暂停、继续或取消当前任务；旧 `attemptId` 和乱序事件不会污染重试任务。
- 保存单个任务到设备。
- 删除单个任务时先中止网络请求，再清理卡片、检查点、分片与 OPFS 成品。
- 打开浏览器下载目录。
- 清空当前任务记录。

### 云端服务体检

1. 打开插件面板。
2. 进入「设置」页。
3. 点击「开始体检」。
4. 插件会优先访问 `/v1/diagnostics`，如果云端服务尚未升级，会自动降级检测 `/v1/status` 和 `/v1/health`。
5. 体检结果会显示总分、整体摘要、分项检查、账号池摘要、服务端建议动作和建议下一步。
6. 如果体检提示账号池为空，可回到「账号池」页上传账号或同步云端。
7. 如果体检提示缺少密钥，需要在 Cloudflare 或本地 `.dev.vars` 中补齐后重新部署。
8. 点击「处理账号池」会自动跳转到账号池页；如果体检发现异常账号，会自动打开失效账号视图。
9. 点击「同步账号」可直接重新同步云端账号池。
10. 点击「复制报告」可把体检结果、账号池摘要、分项检查、建议和服务端建议动作复制到剪贴板，便于保存或排查。
11. 重新打开设置页时会显示当前云端服务地址对应的「上次体检」，可点击「清除上次体检」移除本地历史记录；清除操作不会影响云端账号池和 Worker 服务。

## 视频格式说明

插件使用 `m3u8-parser@7.2.0` 解析清单，并使用 `mux.js@7.0.0` 对 TS 分片进行流式转封装：

- 转封装成功：输出 `.mp4`，MIME 为 `video/mp4`。
- 用户明确选择 TS 时：保留 `.ts` 组装文件。
- 支持 AES-128 正确序列 IV、显式 IV、初始化段、Byte Range、密钥轮换和 discontinuity。
- 直播清单、SAMPLE-AES 或独立音轨无法安全合并时：规划阶段失败并解释原因，不生成伪 MP4、静音文件或损坏文件。

## 本地账号上传云端

1. 进入「账号池」页。
2. 填写本地账号信息。
3. 点击「保存并上传云端」，或在本地账号卡片上点击「上传云端」。
4. 上传成功后，插件会同步云端账号池。
5. 已上传账号会转为云端摘要展示，不能在插件前端查看或修改凭证明文。

## 清除旧数据缓存

5.0.0 覆盖安装不会自动清空运行数据：旧 `fullDetails` 会转换为新的放映历史，账号池、下载任务、Worker 地址和更新设置继续保留。只有购买防重复账本和 `screening` 会升级到新 schema；如果确实需要手动清理：

1. 打开插件面板。
2. 进入「设置」页。
3. 打开「数据管理」分类并点击「完整重置」。
4. 阅读影响范围后点击「确认重置」，完成后刷新页面。

清理范围包括：

- 插件本地账号池缓存
- 远程账号池摘要缓存
- 播放状态缓存
- 下载任务缓存
- 更新检测缓存
- 页面监听运行缓存
- 旧版本默认配置
- 旧默认空种子账号
- 旧测试播放详情缓存

## 开发检查

在当前插件目录运行：

如果修改了 `ui-src/` 中的 Figma Make 界面源码，先执行：

```powershell
npm ci
npm run deps:check
npm run typecheck
npm test
npm run check
npm run check:release-source
npm audit --json
```

如果依赖自检提示 `vite.cmd`、`shaka-player.compiled.js` 或 `shaka-player.transmuxer-worker.js` 缺失，说明 `node_modules` 半损坏，先执行：

```powershell
npm run deps:repair
npm run check
```

仓库不再提供本地模拟预览页。修改 UI 后先生成真实生产资源，再把当前目录作为未打包扩展安装到完整 CDP 权限的 Chrome 开发版；刷新目标网站并直接检查内容脚本、Shadow DOM、Service Worker、扩展 API 和真实媒体行为。所有桌面、手机和横屏验收都以安装后的扩展为准。

```powershell
npm run build
npm run check:release-source
```

```powershell
node --check .\background.js
node --check .\state_mutation_core.js
node --check .\page_context_core.js
node --check .\download_core.js
node --check .\content.js
node --check .\page_hook.js
node --check .\page_probe.js
node --check .\display_patch.js
node --check .\nav_guard.js
node --check .\offscreen_downloader.js
node --check .\save.js
node -e "JSON.parse(require('fs').readFileSync('.\\manifest.json','utf8')); console.log('manifest 正常')"
```

在项目根目录运行：

```powershell
node --check .\tangxin-zhizhe-extension\background.js
node --check .\tangxin-zhizhe-extension\state_mutation_core.js
node --check .\tangxin-zhizhe-extension\page_context_core.js
node --check .\tangxin-zhizhe-extension\download_core.js
node --check .\tangxin-zhizhe-extension\content.js
node --check .\tangxin-zhizhe-extension\offscreen_downloader.js
node --check .\tangxin-zhizhe-extension\save.js
node -e "JSON.parse(require('fs').readFileSync('.\\tangxin-zhizhe-extension\\manifest.json','utf8')); console.log('manifest 正常')"
```

## 常见问题排查

### 页面没有出现悬浮球

1. 确认加载的是 `tangxin-zhizhe-extension` 目录。
2. 确认浏览器扩展页没有报错。
3. 刷新页面。
4. 检查当前页面地址是否匹配 `manifest.json` 的 `matches` 配置。

### 手机端点击按钮无效

1. 刷新页面后重试。
2. 确认没有旧版本插件同时启用。
3. 检查页面是否被其他浮层遮挡。
4. 尝试关闭面板后重新点击悬浮球展开。

### 刷新后仍然看到旧页面

1. 在浏览器扩展管理页点击当前插件的「重新加载」按钮。
2. 确认加载目录中存在 `dist-ui/txzz-ui.js` 和 `dist-ui/txzz-ui.css`。
3. 重新打开或刷新目标页面，不要只刷新扩展管理页。
4. 当前版本已经删除旧 DOM 面板、旧流程浮层和旧更新弹窗；如果仍看到旧面板，通常是浏览器还在运行旧扩展实例，关闭重复安装的旧版本后再刷新页面。

### 构建提示 vite 不是内部或外部命令

1. 在插件目录执行 `npm run deps:check`。
2. 如果提示缺少 `node_modules/.bin/vite.cmd`，说明 `node_modules` 的命令入口损坏。
3. 执行 `npm run deps:repair` 自动修复依赖。
4. 修复完成后重新执行 `npm run build`。

### 构建提示无法解析 Shaka Player

1. 在插件目录执行 `npm run deps:check`。
2. 如果提示缺少 `node_modules/shaka-player/dist/shaka-player.compiled.js` 或转封装 Worker，说明 Shaka Player 包安装不完整。
3. 执行 `npm run deps:repair`，脚本会移除损坏的 `shaka-player` 目录并重新执行 `npm install`。
4. 修复完成后重新执行 `npm run build`。

### 远程账号池同步失败

1. 检查 Worker 地址是否可访问。
2. 检查 Worker 是否配置了 `SUPABASE_URL` 和 `SUPABASE_SERVICE_ROLE_KEY`。
3. 检查 Supabase 是否已执行 `schema.sql`。
4. 访问 `/v1/health` 查看必填变量是否齐全。
5. 在「设置」页点击「开始体检」，根据分项检查处理密钥、数据库或账号池问题。

### 云端服务体检分数较低

1. 如果提示缺少密钥，补齐 Cloudflare Secrets 或本地 `.dev.vars` 后重新部署。
2. 如果提示数据库连接异常，确认 Supabase 地址、`service_role` 和 `schema.sql` 是否正确。
3. 如果提示账号池为空，先上传本地账号或调用 `/v1/accounts/seed` 写入种子账号。
4. 如果提示异常账号较多，打开账号池页的「查看失效」并重新上传凭据。
5. 如果提示待验证账号较多，逐个点击账号检查，减少播放时临时轮换等待。

### 本地账号上传云端后显示无凭据

1. 确认插件版本为 `1.3.0` 或更高版本。
2. 当前版本已保留云端摘要中的 `hasPassword`、`hasQrcode`、`hasToken` 标记。
3. 如果旧云端账号仍显示无凭据，重新上传该账号或在 Worker 部署新版后重新同步云端账号池。
4. 云端账号如果确实没有任何凭据，会显示「不可用：账号没有可用凭据」。

### 保存账号后页面刷新

1. 确认插件版本为 `1.3.0` 或更高版本。
2. 当前版本已将添加账号弹窗中的按钮全部改为普通按钮，并拦截表单默认提交。
3. 如果仍然刷新，请检查是否同时启用了旧版本插件。
4. 关闭旧版本插件后刷新页面，再重新打开「账号池」页保存。

### 播放详情为空

1. 确认账号池中至少有一个可用账号。
2. 同步远程账号池后再试。
3. 确认云端账号中至少有一个可用账号。
4. 查看 Worker 日志确认服务端接口是否返回错误。

### 下载按钮没有反应

1. 进入「下载」页点击「刷新下载」。
2. 确认视频详情页上的下载按钮已经渲染完成。
3. 检查浏览器是否拦截下载。
4. 如果任务正在下载 M3U8 分片，请等待显示「合并完成，待保存」。

### 保存到设备没有弹窗

1. 检查浏览器默认下载目录是否可写。
2. 查看浏览器下载记录。
3. 如果希望每次选择位置，请在浏览器下载设置中开启「下载前询问保存位置」。
4. 如果插件显示 TS 兜底，说明 MP4 转封装失败但文件仍可保存。

### 检查更新不灵敏、云端版本比本地旧、或没有检测到新版

1. 进入「设置 → 版本升级」，点击「实时检查」（升级系统 v8，手动操作始终绕过 15 分钟成功缓存）。
2. 检测会同时请求固定仓库路径的多个清单源（GitHub raw、gitmirror、ghproxy、jsDelivr 等），只有通过正式公钥签名的结果才参与 **version/build** 比较；单个 CDN 的旧缓存或被篡改内容不会覆盖有效新版本。
3. 升级中心会显示缓存年龄、镜像成功/失败数量和逐源版本；自动打开面板时命中成功缓存属于正常现象，点击「实时检查」可立即刷新。
4. 远程 `version` 大于本地 `manifest.json` 的 `version` 时会提醒；版本相同但远程 `build` 晚于本地构建时也会提醒。
5. 本地已是最新时：本地版本与远程版本应一致（或本地构建不低于远程）。若仍显示远程更旧，请确认浏览器已重新加载当前插件目录。
6. 如果显示签名失败，请重新执行 `npm run release` 并完整推送 `update.json` 与 CRX；README 不再作为无签名更新依据。
7. 如果显示检测失败，请确认网络能访问上述任一清单源；可点「复制信息」保存升级系统报告后再排查。

### 下载最新版失败

1. 进入「设置」页，点击「检查更新」后再点「下载」。
2. 插件会实时验证 schema 3 签名清单，再按其中被签名的大小和 SHA-256 获取 **CRX3 安装包**（`releases/tangxin-zhizhe-latest.crx`）。
3. 每个候选地址都必须完全匹配固定 owner/repo/branch/path；完整文件还要通过 SHA-256、CRX3 内嵌公钥、正式扩展 ID、ZIP 起点和包自身 RSA 签名校验，任何一项不符都会切换镜像。
4. 校验后的安装包写入扩展 OPFS，并打开 `save.html` 安全保存页；整个 CRX 不会作为 Base64 放进 runtime 消息，也不依赖可能缺失的 `chrome.downloads.download`。
5. 保存页再次核对文件大小、CRX3 文件头和 SHA-256。同一令牌只能被一个标签领取；支持 File System Access API 时实际写盘成功后才核销，Android Blob 兜底允许重复提交并由用户看到文件后确认。
6. 高级信息会列出每个镜像的 `validated`、`save-page-opened`、`submitted`、`validation-failed` 或 `submit-failed` 状态和具体错误；安全保存页没有浏览器下载 API 编号。
7. **5.0.3 自举例外**：该旧版会在离屏页读取不存在的 `chrome.downloads`，可能报 `Cannot read properties of undefined (reading 'download')`。已安装旧代码无法被远程清单修补，需直接打开 [最新版 CRX](https://github.com/lsy5920/tangxin-zhizhe-extension/raw/main/releases/tangxin-zhizhe-latest.crx) 手动覆盖一次。
8. 如果保存页被关闭，可回到升级中心点击「重新验证并打开保存页」生成新票；若仍失败，复制完整镜像与校验报告。CRX 保存完成也不等于安装完成，仍需在扩展管理页手动安装或覆盖更新。

### 开发者如何打 CRX 包（发版）

1. 确认本机已安装 Node.js，并在项目目录执行过 `npm install`。
2. 开发源码升级时先同步：`manifest.json`、`package.json`、`package-lock.json`、`ui-src/app/constants.ts`、`background.js` 本地构建号和 `README`；没有正式私钥时不要修改 `update.json`、`latest.json` 或 CRX。
3. 正式发版时从离线备份恢复 `keys/txzz-extension.pem`，再把 `update.json` 的版本、构建号和首条日志更新到源码版本；打包脚本会计算安装包哈希并重新签名。除非原私钥确认永久丢失且接受扩展 ID 变化，否则禁止生成替代身份。
4. 执行一键发版构建：

```bash
npm run release
```

该命令会依次执行依赖自检、TypeScript 检查、生产构建、源码身份检查、CRX3 打包、外置清单签名和完整发布校验。最后一步会验证 `update.json` 的固定公钥签名、CRX3 自身签名和正式 ID，解析 ZIP 中央目录并验证 CRC32，再把 CRX 内每个运行时文件与当前工作区逐字节核对；任一项不一致都会终止发版。

5. 产物在 `releases/`：
   - `tangxin-zhizhe-latest.crx`（更新系统固定下载这个）
   - `tangxin-zhizhe-{版本号}.crx`（带版本备份）
   - `extension-id.txt`、`latest.json`、`signing-public-key.json`
6. `keys/txzz-extension.pem` 必须从安全备份准备好且禁止提交。私钥缺失或不对应正式 ID 时打包会直接失败，程序绝不会自动生成替代身份。
7. 将 `releases/*.crx`、签名清单与源码一并提交并推送到 `main`，用户即可在线下载更新。

## 安全与隐私

- 不要把账号密码、二维码凭证、token、deviceId 写入插件源码。
- 不要把 Supabase `service_role` 写入插件源码。
- 插件内置访问密钥只用于识别配套客户端，不承载 Supabase 管理权限；真正的数据库和账号加密密钥仍必须只保存在 Worker 或 GitHub Secrets 中。
- 不要提交 `.env`、`.dev.vars`、日志、截图、浏览器 profile 和运行证据。
- 已经暴露过的密钥应立即轮换。
- 云端账号凭据应由 Worker 加密保存，插件只展示脱敏摘要。

## 版本说明

| 组件 | 版本 |
| --- | --- |
| 糖心志者源码 | `5.7.0`，构建 `2026-07-31-0145` |
| 正式签名 CRX | `5.7.0`，扩展 ID `ddefadnhgebdclpkabeobjidjllkdkhm` |
| Manifest | `3` |
| Shaka Player 播放内核 | `5.2.4` |
| mux.js | `7.0.0` |
| m3u8-parser | `7.2.0` |
| Worker 推荐版本 | `2.1.0` |
| Wrangler 推荐版本 | `4.110.0` |

## 更新日志

[2026-07-31 01:45] 【重构】升级到 `5.7.0`（构建 `2026-07-31-0145`）：插件面板收敛为今日总览、照料中心、账号小屋与影院入口，片库、书签、足迹、播放、下载和 OPFS 存储全部归入独立糖心影院。影院重新设计为专业影视 App 布局，重写目录/详情/放映控制器、请求代次和跨页 ViewModel；播放器控制栏、设置侧栏、画面选集、长按/横滑手势及松手提交的目标帧预览统一重构。下载规划票据和入队错误留场同步修复，移动端主视觉标题限制为三行并为操作按钮、海报重新分配空间。Worker 保持 `2.1.0`，正式扩展 ID 与私钥不变。

[2026-07-30 21:36] 【重构】升级到 `5.6.0`（构建 `2026-07-30-2136`）：糖心影院从网站工作台覆盖层迁移到独立 `cinema.html` 浏览器标签页，桌面使用可折叠影视侧栏，移动端使用首页、发现、搜索、片库、足迹、下载六项安全区底栏；同一份 Vite 生产资源同时驱动工作台和影院，移除双入口视觉漂移。后台通过 `runtime.getContexts` 识别并复用浏览器恢复或直接打开的影院标签，保持原工作台、放映室、账号和下载能力不变。

[2026-07-30 21:36] 【修复】完整 CDP Chrome Dev 实测修复全新安装空会话读取 `userToken`、独立 `txzzExperienceV1` 片库被合集/海报校验误读、筛选或搜索期间短暂展示旧片单，以及目录缺少合集标记时无法识别分集的问题；用户打开详情后统一白名单化 `groups` 元数据，普通单片不显示选集，合集仍不保存播放 URL或触发购买。生产样式覆盖块移到正式 CSS 末尾，避免旧影院规则覆盖独立页背景、分隔线、动画和响应式布局。

[2026-07-30 11:22] 【功能】升级到 `5.5.0`（构建 `2026-07-30-1122`）：糖心影院重构为独立沉浸式影视 App，新增完整首页/发现/搜索/片库/足迹/详情/播放路由、海报滚动记忆与合集选集。合集可在视频画面直接换集，免费/VIP 分集自然结束后 5 秒续播，金币分集必须再次确认；切集始终重新检票并由媒体代次隔离旧事件。详情页、合集当前集、播放器画面、影片侧栏和片源/下载抽屉均可进入同一 OPFS 下载规划器，点击后立即显示探测态，失败可原位重试。封面解密增加有限重试与焦点恢复；Worker 保持 `2.1.0`，正式扩展 ID 和私钥不变。

[2026-07-30 01:38] 【功能】升级到 `5.4.0`（构建 `2026-07-30-0138`）：新增独立糖心影院，目录全部来自目标站原始列表接口，支持发现分区、搜索、筛选、分页海报墙、影片详情、收藏和稍后看。目录阶段严格排除广告区块与完整播放 URL，不轮换账号、不请求完整详情、不触发购买；用户明确点击开映后才使用独立影院代次进入现有完整播放会话，避免被网页 Vlog 当前卡片切换判为过期。真实 CDP 验收同步修复长检票等待被前端过早终止、检票新片时仍显示旧舞台，以及新 active session 到达后足迹选择仍指向上一部的问题。Worker 保持 `2.1.0`。

[2026-07-30 00:14] 【重构】升级到 `5.3.0`（构建 `2026-07-30-0014`）：播放内核从 ArtPlayer + hls.js 完整迁移到 Shaka Player 5.2.4，统一 HLS、AES-128、ABR、错误恢复和 TS 转封装；MV3 使用扩展本地 transmux Worker。拖动进度条和横滑改为事务式预览：主视频固定在手势起点，独立低码率会话解码目标帧，进度点与小画面同步跟随，松手后只提交一次跳转；同步修复并发装载复活旧实例、装载错误覆盖备用线、滑回起点卡住播放及全屏规则误显示隐藏预览视频，并降低移动端缓冲与解码负担。

[2026-07-29 20:30] 【修复】升级到 `5.2.2`（构建 `2026-07-29-2030`）：9:16 视频改用动态视口预算舞台，设置菜单精简为片源/观看/工具；修复旧会话缺少 `acquisition` 导致下载规划读取 `mode` 崩溃、旧成功缓存造成同版本误报，以及 Service Worker 调用未定义 `parseBuildStamp` 导致实时更新检查失败的问题。

[2026-07-29 20:30] 【体验】新增可持久化标准/镜像双击与长按布局：标准左退右进，镜像左进右退并同时支持长按快进和长按快退；横向滑动仍固定左划回退、右划前进。横滑和直接拖动进度条共用同一主 video 解码输出，进度条小画面跟随进度点并固在上方。删除本地模拟预览页，所有验收统一改为安装生产扩展后在完整 CDP Chrome 中执行。

[2026-07-29 19:29] 【修复】升级到 `5.2.1`（构建 `2026-07-29-1929`）：修复 Tailwind 4 的 `@property` 在 Chrome Shadow DOM 中未稳定注册，导致正式扩展批量丢失边框、横线、渐变、阴影、位移和部分动画；影子树显式初始化完整的低优先级属性基线，同构建号的残缺样式根会自动重建。

[2026-07-29 18:10] 【体验】升级到 `5.2.0`（构建 `2026-07-29-1810`）：新增片库收藏/稍后看、播放书签与 A-B 片段、智能下载调度、OPFS 存储管家和账号健康巡检；播放器右键菜单在 ArtPlayer 视频元素上也统一生效，书签迁移与 OPFS 全量审计更加稳健。

[2026-07-28 22:27] 【体验】升级到 `5.1.3`（构建 `2026-07-28-2227`）：移除网站左上角的播放资源流程气泡与 Vlog 当前卡片电影票标签；后台当前卡片锁定、完整线路获取和资源记录逻辑保持不变，并主动清理升级后可能残留的旧 HUD。

[2026-07-28 09:50] 【修复】升级到 `5.1.2`（构建 `2026-07-28-0950`）：修复系统全屏 Esc 退出竞态和全屏按钮持焦点时快捷键失效；全屏事务只在浏览器确认退出后恢复内嵌布局，退出失败会保留全屏壳并允许重试，L 锁屏、播放器设置 Esc 与 CSS 沉浸兜底均完成回归。

[2026-07-28 09:50] 【体验】弹窗和下载规划器移到 ShadowRoot 顶层 Portal，修复 inert 连带禁用、Esc 后焦点不恢复与 Toast 遮挡播放器；统一 44×44 触控热区、动态视口、安全区和 844×390 矮横屏布局，并把 CRX 安全保存成功按钮明确为“保存完成”。

[2026-07-28 09:50] 【安全】发布链路移除带来高危 `archiver/glob/minimatch` 传递依赖的 `crx@5.0.1`，改用仓库内可审计、可复现的 ZIP + CRX3 RSA 打包器；Vitest 升级到 `3.2.7`，新增 CRX3 身份、签名、篡改拒绝、路径穿越和大小写冲突测试，`npm audit` 降为 0 项。

[2026-07-27 19:25] 【重构】升级到 `5.1.0`（构建 `2026-07-27-1925`）：Vlog 当前卡片改为活动 DOM、播放器 ID、Vue `isActive` 与 Swiper 索引交叉锁定，旧 ID → 空值 → 新 ID 的换片窗口不再允许旧请求写回；网站播放器、插件播放器、复制和下载统一使用 v2 推荐线路，并新增电影票 HUD 与手动重新同步。

[2026-07-27 19:25] 【体验】新增省流、均衡、高清播放模式和可见质量决策；播放器按媒体 URL 指纹处理同会话换源，HLS 恢复次数按线路隔离，MediaKernel 每次装载只发一次 `ready`，Wake Lock 延迟取得后会复核当前会话。

[2026-07-27 19:25] 【下载】新增下载前规划器、OPFS 可暂停/继续/取消/重启恢复队列、`attemptId + sequence` 事件隔离，以及 AES-128、Map、Byte Range、密钥轮换和 discontinuity 支持；206 分片必须严格匹配 `Content-Range` 与字节数，忽略 Range 的 200 响应只有覆盖目标区间才可截取；直播、SAMPLE-AES 与独立音轨在规划阶段安全拒绝。

[2026-07-27 19:25] 【安全】新增云端/本地金币购买对账中心与扩展安全保存页。视频和 CRX 成品从 OPFS 保存，一次性令牌同时只允许一个标签领取，旧标签关闭后可由新标签安全接管；CRX 再次校验大小、CRX3 头和 SHA-256，不再通过 runtime 响应传输整包 Base64，也不会把被浏览器拦截的点击误报为已保存。

[2026-07-27 22:25] 【修复】最终发布回归补齐 Byte Range 响应区间/长度校验、下载进度缓冲的跨 `attemptId` 隔离、持久任务恢复计划测试、安全保存票关闭标签后接管，以及下载规划器顶层焦点、Esc、Tab 循环与底层工作台 inert；扩展 Vitest 增至 10 个文件、35 项并全部通过。

[2026-07-27 13:15] 【修复】升级到 `5.0.5`：确认 5.0.3 在离屏页读取缺失的 `chrome.downloads` 导致最新版下载失败；新版在后台下载 API 缺失或拒绝时，把同一份已完成大小、SHA-256、扩展 ID 与 CRX3 签名验证的字节交给当前页面复核后以 Blob 保存。Vlog 回填同时改为优先使用实际探测后的推荐完整线路，并把候选线路统一规范为网站原生 `changeSources` 所需的 `line.link`。

[2026-07-27 12:51] 【修复】升级到 `5.0.4`：完整检票结果现在会回填网站 `/vlog/` 当前 Vue 卡片，并调用活动 ArtPlayer/Hls 的 `changeSources` 将网站原生试看 Blob 切换为完整线路，同时保留当前播放/暂停状态和进度；修复离屏页缺少 `chrome.downloads` 导致更新失败，升级下载改为后台直接以 `saveAs:true` 提交已校验 CRX 字节。

[2026-07-27 10:15] 【修复】升级到 `5.0.2`：为 Vlog 播放请求加入点击项、SPA 路由代次和上下文键隔离，预加载/下一个视频的详情不能再覆盖当前放映；完整线路探测改为读取完整 HLS 清单，兼容 206/Range 截断并延长慢线路探测窗口；同时扫描 `lines[]` 等嵌套线路，按每条视频实际清单时长选择完整版本，修正 17 分钟短清单覆盖不同长度视频的问题。

[2026-07-27 08:51] 【修复】升级到 `5.0.1`：主备 HLS 在会话返回前完成媒体清单与变体清单探测，按每个视频的相对覆盖时长优先选择完整版，修复短预览线因低延迟或“主线优先”覆盖较长完整线的问题；本地账号模式、缓存会话纠偏、自动切线和下载选线使用同一规则。
[2026-07-27 08:51] 【体验】片源抽屉显示每条线路的实际探测时长，并为因覆盖更完整而被选中的线路标记“完整版优先”；新增 `duration-mismatch` 确定性预览和相关回归测试，覆盖 17 分钟主线与 1 小时备用线的相对选择，但业务规则不依赖固定时长。
[2026-07-27 01:00] 【重构】发布沉浸糖果影院 5.0：播放页拆为页面编排、单 reducer 会话状态机、单 ArtPlayer/Hls 媒体内核、线路策略、全屏控制、偏好与续播存储，以及主舞台、影片侧栏和片源/下载/足迹抽屉；资源就绪后默认暂停，所有旧媒体与全屏回调按会话代次隔离。
[2026-07-27 01:00] 【安全】扩展只调用 Worker v2 播放会话接口，本地账号模式新增 `pending / charged / resolved / failed_before_charge / uncertain` 五态购买账本；任何账号已有主线或备用线时禁止购买，扣费后详情失败也不会自动二次购买。
[2026-07-27 01:00] 【发布】升级到 `5.0.0`（构建 `2026-07-27-0100`），继续使用正式私钥和扩展 ID `ddefadnhgebdclpkabeobjidjllkdkhm`；新增 Vitest 状态机、迁移、线路策略和续播测试，并完成桌面、移动、横屏、故障切线、右键、键盘和沉浸全屏兜底验收。

2026-07-08 20:38 【修复】升级版本到 v2.9.1，修复全屏播放画面偏右、左右黑边不均和圆角残留问题；全屏模式会清理父层位移、模糊和圆角残留，播放器壳层、ArtPlayer 容器和 video 标签统一贴合真实视口，默认原比例下按屏幕中心完整显示视频；播放器诊断报告新增容器偏移、视频层尺寸、视频层偏移和左右黑边差值，工具菜单新增「居中」按钮，可一键重新接管全屏并校正画面；同步对齐 manifest、package、前端常量、后台构建号和 update.json 到 v2.9.1，避免升级检测读取到旧版本。
2026-06-09 01:22 【修复】修复插件中文编码损坏问题，重建面板文案和基础文档，避免页面出现乱码和裸露标签。
2026-06-09 09:06 【新增】新增「清除数据缓存」功能，支持覆盖安装后清理旧账号池、旧完整详情、旧链路记录、远程配置和页面运行缓存。
2026-06-09 09:18 【修复】修复手机版面板顶部按钮点击无效、滚动后无法回到完整顶部栏的问题，移动端改为可视窗口固定布局。
2026-06-09 16:21 【修复】修复云端账号池同步失败时没有清晰提示的问题，新增「固定云端账号」模式和坏账号轮换兜底。
2026-06-09 21:35 【优化】默认 Worker 地址切换为自定义域名，云端账号改为只读摘要显示；本地账号卡片新增「上传云端」按钮。
2026-06-09 23:40 【新增】新增完整视频下载能力，接管详情页下载按钮并优先使用完整账号池获取完整播放链接。
2026-06-10 00:55 【新增】新增「下载」独立管理页面，支持查看当前任务、分片进度、失败原因、复制下载数据、保存任务快照、清空任务记录和打开下载目录。
2026-06-10 01:39 【修复】修复部分浏览器中保存流程不稳定的问题，新增隐藏链接兜底保存链路。
2026-06-10 02:02 【优化】新增 `mux.js@7.0.0` 转封装能力，M3U8 完整下载优先输出真实 MP4 文件，失败时自动保留 TS 兜底。
2026-06-12 23:36 【优化】重写插件 README 为 GitHub 风格文档，补充项目介绍、环境要求、安装教程、使用教程、目录结构、常见问题、安全说明和版本说明。
2026-06-12 23:36 【优化】重构账号池页面为控制台布局，统一云端账号卡片展示账号昵称、可用状态和不可用原因；失效云端账号默认隐藏并可手动查看；新增「添加账号」入口，支持账号密码、账号凭证和 token/deviceId 三种本地保存方式。
2026-06-13 00:08 【修复】修复点击保存账号或保存并上传云端时触发表单默认提交导致页面刷新的问题；添加账号改为先弹出类型选择，再进入对应填写弹窗，只显示当前类型需要填写的内容。
2026-06-13 00:13 【修复】修复本地账号上传云端后同步回来显示「无凭据」并被判定不可用的问题，插件后台保留云端摘要凭据标记，账号卡片可正确显示账号密码、账号凭证或 token/deviceId 状态。
2026-06-13 00:27 【优化】简化远程账号池配置，插件界面不再要求填写旧版读取令牌或上传令牌；账号卡片新增普通 VIP、尤物圈和金币余额三项权益摘要，云端账号同步后同样展示。
2026-06-13 01:08 【优化】重写插件界面文案和 README 正文术语，统一改为账号池、播放资源、播放详情、账号检查和下载任务等专业表达，去除旧的测试场景描述。
2026-06-13 01:18 【修复】修复悬浮球按钮因点击动作拦截导致无法拖动的问题；主界面新增「关于」按钮，点击后打开 GitHub 项目主页，同步更新使用教程。
2026-06-13 01:31 【优化】移除固定云端账号选择功能；云端账号改为按金币数量升序自动轮换，金币视频先轮询所有账号是否已购买，全部未购买时从金币最少账号组中随机选择账号购买，失败后继续切换下一个账号。
2026-06-13 01:33 【优化】移除云端账号卡片上的「选择」按钮，云端账号统一显示「自动轮换」只读状态；本地账号保留选择、检查、编辑和上传云端能力。
2026-06-13 01:45 【优化】增强当前使用云端账号卡片视觉效果，新增「最近使用」和「自动轮换」标签；页面左上角新增关键流程提示，只显示插件正在执行的核心步骤。
2026-06-13 01:58 【优化】左上角关键流程提示新增云端账号操作状态，播放详情和下载流程会显示正在轮换、实际使用账号、尝试数量、失败切换数量和金币解锁策略。
2026-06-13 02:06 【优化】关键流程提示改为独立页面标签，仅在插件面板未打开时显示；用户直接在网页中操作视频或下载时，左上角会显示云端账号和播放资源流程。
2026-06-13 02:14 【优化】关键流程提示改为队列顺序播放，非错误提示默认显示 2 秒，错误提示保留更久，避免流程提示互相覆盖。
2026-06-13 02:23 【新增】新增远程仓库更新日志检查能力，插件会自动对比 GitHub README 更新日志，发现新记录时弹出精美更新提醒并可跳转项目主页。
2026-06-13 02:31 【优化】更新提醒改为使用插件文件夹内 README.md 和远程仓库 README.md 全量对比更新日志，只要远程出现本地没有的新日志就提醒。
2026-06-13 09:10 【新增】写入远程更新提醒测试日志，用于验证插件对比 GitHub README 更新日志后弹出更新提醒。
2026-06-13 09:18 【优化】更新提醒启动时强制实时检查远程 README，工具页新增「检查更新」按钮，便于推送新日志后立即验证弹窗。
2026-06-13 09:24 【新增】写入第二条远程更新提醒测试日志，用于验证远程 README 出现新增记录时插件弹窗提醒是否正常。
2026-06-13 09:44 【优化】更新提醒改为优先读取远程 `update.json` 版本清单，对比版本号、构建号和日志 ID；README 更新日志对比保留为备用方案。
2026-06-13 09:50 【新增】更新远程版本清单测试版本到 `1.3.3`，用于验证插件已提醒旧版本后仍能识别新的版本号、构建号和日志 ID。
2026-06-13 10:16 【优化】全面重构糖心志者面板 UI，新增总览页、桌面端左侧导航、右侧工作区和移动端底部导航，统一深色专业控制台视觉。
2026-06-13 10:21 【修复】修复账号池未勾选「查看已失效云端账号」时仍显示待检查云端账号的问题，云端账号默认仅展示明确可用账号。
2026-06-13 10:33 【优化】使用内置图片模型生成并接入面板美化素材，新增总览页品牌插画、空状态插画和功能图标素材，提升插件面板视觉质感。
2026-06-13 11:13 【优化】根据 PC 和移动端 UI 设计稿重构糖心志者面板布局，桌面端改为左侧图标轨加紧凑工作区，移动端改为手机窗口、顶部标题栏和底部图标导航，并接入页面插图、导航图标和移动端纹理素材。
2026-06-13 11:28 【修复】修复页面插图和导航图标只通过相对路径背景引用导致显示不稳定的问题，改为通过插件运行时绝对地址注入素材变量，并为八个页面添加真实插画节点；同步强化桌面端图标轨、工具宫格、账号池网格和移动端手机窗口布局。
2026-06-13 11:35 【修复】修复导航图标精灵图原始格子为非正方形导致圆形按钮内裁剪偏移的问题，新增标准方形图标精灵图并切换插件导航和工具宫格统一使用。
2026-06-13 12:04 【优化】使用图片模型重新生成八个独立导航图标、悬浮球图标和流程标签背景图，导航栏、工具宫格、悬浮球和页面流程标签全部改为直接引用独立图片资产，彻底移除导航图标精灵图裁剪依赖。
2026-06-13 12:07 【优化】放大桌面端侧栏图标、移动端底部导航图标、工具宫格图标和悬浮球图标显示尺寸，提升小尺寸面板中的图标识别度。
2026-06-13 13:36 【修复】修复流程标签背景图因独立浮层未继承面板素材变量导致不生效的问题，标签显示时会直接应用新背景图，避免继续显示旧纯色样式。
2026-06-13 13:36 【优化】完善更新提醒系统，启动时静默预热远程版本清单，检测到更新后在面板内显示提示条，每次打开面板都会弹出更新弹窗，点击提示条可再次查看详情。
2026-06-13 13:42 【新增】糖心志者版本号升级到 `2.0.0`，同步更新插件清单、远程版本清单、本地构建号、版本说明和更新日志。
2026-06-13 13:45 【新增】更新提醒弹窗新增「下载最新版」按钮，点击后直接下载 GitHub 仓库 main 分支最新压缩包，同时保留查看更新和稍后提醒入口。
2026-06-13 13:49 【新增】糖心志者版本号升级到 `2.0.1`，同步更新插件清单、远程版本清单、本地构建号和版本说明。
2026-06-13 13:52 【新增】糖心志者版本号升级到 `2.0.2`，用于测试更新提醒弹窗直接下载 GitHub 仓库最新压缩包功能。
2026-06-13 13:58 【新增】糖心志者版本号升级到 `2.0.3`，更新日志：测试下载功能。
2026-06-13 14:20 【优化】精简插件功能界面，移除资源、账号状态、对比和调试工具入口，保留总览、账号池、播放、下载和设置五个页面；同步更新版本清单、使用教程和版本说明。
2026-06-14 01:06 【修复】修复云端账号因视频下架、播放详情缺少链接等资源异常被误判为失效的问题；账号池默认展示逻辑改为保留有成功检查记录的可用账号，同步更新版本清单和 README 说明。
2026-06-14 01:23 【优化】清理 Supabase 云端账号池旧数据，删除探针账号、空凭据账号、过期导入账号和重复用户编号记录，账号池保留 3 个唯一可用账号。
2026-06-14 02:57 【优化】根据 Figma Make 糖果风设计稿重做插件面板视觉，应用粉紫渐变、白色卡片、圆润按钮、糖果悬浮球、更新弹窗和移动端全屏适配，同步更新 README 说明。
2026-06-14 03:55 【优化】直接接入 Figma Make「生成糖果风页面」React 源码，新增 ui-src 源码目录、Vite 固定版本构建链路和 dist-ui 扩展加载产物；content.js 新增状态与动作事件桥，保留原账号池、播放、下载和更新业务逻辑；manifest.json 加载新糖果风 UI，同步更新 README 构建与目录说明。
2026-06-14 04:03 【修复】修复糖果风 React 内容脚本因构建产物残留运行环境判断导致浏览器报错、页面继续显示旧界面的问题；旧 DOM 面板改为隐藏业务承载层，新界面提升到最高层级并增强状态桥重试，同步更新 README 排查说明。
2026-06-14 09:59 【修复】修复糖果风 React 界面关闭状态仍显示整屏背景导致网站内容被遮挡的问题，关闭时只保留流程标签和悬浮球，打开面板时保留模糊蒙版；补充 Shadow DOM 圆角变量与圆角类兜底，修复按钮和背景色块变直角的问题；播放页动作标签改为中文显示，同步更新 README 说明。
2026-06-14 10:03 【新增】版本号升级到 `2.1.2`，同步更新 manifest、package、update.json、后台本地构建号、前端版本展示和 README 版本说明。
2026-06-14 14:47 【修复】修复糖果风账号池页面云端账号摘要识别不完整的问题，前端现在会按后台同一规则识别远程账号、二维码云端账号、种子账号和只读云端摘要，避免云端账号误显示在本地账号分类下。
2026-06-14 14:58 【新增】版本号升级到 `2.1.3`，同步更新 manifest、package、update.json、后台本地构建号、前端版本展示和 README 版本说明，便于用户重新加载插件后确认云端账号分类修复已生效。
2026-06-14 15:15 【删除】整理插件文件和历史数据，删除当前 Figma Make 糖果风界面不再使用的旧图片素材目录，移除旧默认空种子账号和测试播放详情数据，同步升级到 `2.1.4` 并刷新本地缓存结构版本。
2026-07-04 18:00 【优化】升级版本到 v2.2.0：全面重构五个 UI 页面组件，总览页状态卡片改为可点击跳转并显示相对时间；账号页账号卡片新增验证按钮、tokenMasked 展示和上次验证时间；播放页新增复制 M3U8 链接按钮和播放 URL 预览；下载页新增复制链接按钮和保存记录按钮，任务分片数显示更精确；设置页新增 Worker 健康检测按钮（直接 ping /v1/health）和展示覆盖快捷应用入口。
2026-07-04 18:00 【优化】升级 App.tsx 主组件：新增 Escape 键关闭面板快捷键；悬浮球状态指示圆点改为三态（绿色=空闲，橙色=下载中，黄色=有更新）；有进行中下载任务时在悬浮球和底部导航显示数字徽章；流程消息提示条颜色根据 ok/error/running 等级动态变化；导航栏版本号 v2.2.0 显示已更新。
2026-07-04 18:00 【新增】helpers.ts 新增 formatRelativeTime（相对时间描述，如"3分钟前"）和 maskUrl（URL 安全截断，仅显示域名和末尾路径）两个工具函数，供各页面组件调用。
2026-07-07 23:33 【新增】升级版本到 v2.3.0，设置页新增云端服务体检面板，支持读取 Worker 智能诊断结果并展示密钥、数据库、账号池、异常账号和待验证账号分项状态；账号池页统一「云端服务地址」文案，同步更新 update.json、构建产物和 README。
2026-07-07 23:48 【优化】优化云端服务体检后的处理流程，新增「处理账号池」「同步账号」「复制报告」快捷按钮；从体检页跳转账号池时可自动打开失效账号视图或添加账号入口，减少排查步骤。
2026-07-07 23:53 【优化】优化云端服务体检体验，新增上次体检结果本地记忆，并和当前云端服务地址绑定；设置页再次打开时可自动展示最近体检状态，并支持一键清除上次体检记录。
2026-07-08 00:03 【优化】升级版本到 v2.3.1，优化视频播放体验，播放页新增播放就绪面板、主备线路状态、刷新播放资源入口和复制完整链接能力；同步优化设置页更新管理，展示本地版本、远程版本、构建号和更新说明。
2026-07-08 00:08 【优化】升级版本到 v2.3.2，继续优化视频播放和下载体验，播放页新增当前视频下载任务联动，可直接查看下载阶段、分片进度、文件大小、更新时间，并支持保存到设备或跳转下载页；设置页更新管理新增「复制信息」按钮，便于复制本地版本、远程版本、构建号和更新说明。
2026-07-08 00:11 【优化】升级版本到 v2.3.3，修正播放链接复制逻辑，主线路、备用线路、最近播放链接和最新播放链接都会复制包含域名的完整绝对地址，避免复制到只有路径的相对链接。
2026-07-08 00:16 【优化】升级版本到 v2.3.4，继续优化视频下载体验，下载页任务卡片新增完整源链接摘要，复制下载链接时会补全为包含域名的完整绝对地址；同步更新远程版本清单和升级说明。
2026-07-08 00:31 【优化】升级版本到 v2.3.5，继续优化视频播放记录体验，播放记录卡片新增复制完整链接、刷新该视频资源、下载该视频和下载状态展示；同步更新远程版本清单和升级说明，提交后直接同步到远程仓库。
2026-07-08 00:40 【优化】升级版本到 v2.3.6，继续优化视频下载展示体验，下载页新增全部、进行中、可保存和失败四类筛选，并修正进行中统计口径；同步更新远程版本清单和升级说明，提交后直接同步到远程仓库。
2026-07-08 00:44 【优化】升级版本到 v2.3.7，继续优化视频下载链接复制体验，下载页新增「复制筛选链接」按钮，可将当前筛选结果中的完整下载源链接按行批量复制；同步更新远程版本清单和升级说明，提交后直接同步到远程仓库。
2026-07-08 00:48 【优化】升级版本到 v2.3.8，继续优化视频下载失败处理体验，下载页失败任务新增「重试」入口，可直接重新创建同一视频的下载任务；同步更新远程版本清单和升级说明，提交后直接同步到远程仓库。
2026-07-08 00:51 【优化】升级版本到 v2.3.9，继续优化视频下载失败恢复体验，下载页失败筛选新增「重试失败」入口，可按视频编号去重批量重新创建失败下载任务；同步更新远程版本清单和升级说明，提交后直接同步到远程仓库。
2026-07-08 00:55 【优化】升级版本到 v2.4.0，打通播放页当前视频下载操作，当前视频下载卡片新增完整源链接摘要、复制链接和失败重试入口；同步更新远程版本清单和升级说明，提交后直接同步到远程仓库。
2026-07-08 01:00 【优化】升级版本到 v2.4.1，播放页新增播放资源体检卡片，聚合主备线路可用性、分片数量、时长和探测异常，直接展示推荐线路、体检分、风险提示和刷新建议；同步更新远程版本清单和升级说明，提交后直接同步到远程仓库。
2026-07-08 01:09 【优化】升级版本到 v2.5.0，全面重构升级系统，更新检测改为实时读取远程版本清单，无更新也展示远程版本、检测来源、检测时间和下载地址；修复下载最新版流程，下载前先检测远程清单并使用 GitHub 主分支压缩包候选地址兜底；升级后旧账号、播放、下载和更新缓存不再沿用；播放页同步新增一键复制播放资源体检报告，提交后直接同步到远程仓库。
2026-07-08 01:22 【优化】升级版本到 v2.5.1，继续优化视频下载收尾体验，下载页新增「保存全部」入口，可按当前筛选逐个处理全部可保存任务；修正可保存统计口径，已保存任务不再占用可保存数量；同步更新远程版本清单和升级说明，提交后直接同步到远程仓库。
2026-07-08 01:30 【优化】升级版本到 v2.5.2，继续优化视频下载排查体验，下载页新增「复制报告」入口，可按当前筛选复制任务状态、格式、进度、文件大小、更新时间、失败原因和完整源链接；同步补齐下载阶段中文状态函数，避免下载流程提示缺失；同步更新远程版本清单和升级说明，提交后直接同步到远程仓库。
2026-07-08 01:36 【优化】升级版本到 v2.5.3，继续优化视频播放记录续操作体验，播放记录卡片主按钮会按对应下载任务状态智能切换为「下载」「保存」或「重试」，减少用户在播放页和下载页之间反复切换；同步更新远程版本清单和升级说明，提交后直接同步到远程仓库。
2026-07-08 01:41 【优化】升级版本到 v2.5.4，继续优化视频播放记录排查体验，播放记录卡片新增「报告」入口，可复制单条播放记录的标题、编号、账号、主备完整线路、分片、时长、探测异常和关联下载任务状态；同步更新远程版本清单和升级说明，提交后直接同步到远程仓库。
2026-07-08 01:47 【优化】升级版本到 v2.5.5，继续优化视频播放记录查找体验，播放记录新增搜索框和「全部、可下载、可保存、失败、有备用」五类筛选，进行中的记录主按钮会跳转下载页查看进度，设置页更新管理新增「复制地址」入口；同步更新远程版本清单和升级说明，提交后直接同步到远程仓库。
2026-07-08 01:55 【优化】升级版本到 v2.5.6，继续优化视频下载任务查找体验，下载页新增搜索框，可按标题、视频编号、任务编号、阶段、失败原因和完整链接搜索当前筛选结果；批量保存、复制链接、复制报告和重试失败都会只处理搜索后的任务；设置页更新管理新增「打开地址」入口，提交后直接同步到远程仓库。
2026-07-08 02:03 【修复】升级版本到 v2.5.7，全面重构升级系统检测与最新版下载链路，修复远程清单检测成功后因状态写入字段错误导致检测失败的问题；新版会实时绕过缓存读取 update.json，旧更新缓存不再沿用，下载最新版优先使用清单里的完整下载地址并自动尝试候选压缩包地址；设置页新增检测模式、清单地址、候选下载地址和下载失败原因展示，提交后直接同步到远程仓库。
2026-07-08 02:16 【优化】升级版本到 v2.5.8，继续优化视频下载展示体验，下载页新增「最近、失败优先、进度、大小」排序切换，批量保存、复制链接、复制报告和重试失败都会按当前筛选、搜索和排序后的可见任务处理；设置页更新管理补充自动检测保护间隔和手动检查绕过缓存说明，提交后直接同步到远程仓库。
2026-07-08 02:20 【优化】升级版本到 v2.5.9，继续优化视频下载失败排查体验，下载页新增失败原因概览，按当前筛选、搜索和排序后的任务聚合失败原因数量；新增「失败摘要」复制入口，可导出失败原因、视频编号和完整源链接；批量复制链接和任务报告现在会严格按当前可见顺序输出，提交后直接同步到远程仓库。
2026-07-08 02:26 【优化】升级版本到 v2.6.0，继续优化视频播放记录排查体验，播放页新增「批量报告」入口，可按当前搜索和筛选结果一次性复制播放记录批量报告，报告包含可播放数量、异常数量、备用线路数量、主备完整链接、关联下载任务、失败原因和完整下载源链接，提交后直接同步到远程仓库。
2026-07-08 02:32 【优化】升级版本到 v2.6.1，继续优化视频播放记录展示体验，播放页新增「最近、异常、可保存、备用」排序切换，播放记录列表和批量报告都会按当前搜索、筛选和排序后的可见顺序输出，方便优先处理异常记录、可保存任务或带备用线路的视频，提交后直接同步到远程仓库。
2026-07-08 02:42 【优化】升级版本到 v2.6.2，全面重构升级检测和最新版下载链路：更新系统提升到 v3，旧运行数据和旧更新缓存不再沿用；检查更新会等待本次远程清单结果返回后再弹窗，并用 README 更新日志兜底识别清单漏更；下载最新版会重新检测远程清单、给实际下载地址追加时间戳并自动尝试多个 GitHub 压缩包候选地址；设置页同步展示缓存策略、下载策略、实际尝试地址、服务端诊断账号摘要和下一步动作，提交后直接同步到远程仓库。
2026-07-08 08:02 【优化】升级版本到 v2.6.3，查阅 hls.js、Video.js、Shaka Player、dash.js 等热门开源视频播放内核后，选择并嵌入更适合 HLS/m3u8 的 hls.js 1.6.16；播放页新增网页播放控制台，支持推荐线路、主线路、备用线路和历史播放记录直接预览，新增播放器重载、备用线路切换、画中画、当前预览完整链接复制和完整链接打开；设置页同步展示播放内核版本和升级系统 v3 信息，继续保持复制链接为包含域名的完整链接，提交后直接同步到远程仓库。
2026-07-08 08:21 【优化】升级版本到 v2.7.0，将播放页从原生 video 预览升级为 ArtPlayer 5.4.0 + hls.js 1.6.16 完整播放器方案，补齐进度拖动、快进快退、倍速、热键、截图、画中画、网页全屏、浏览器全屏、断点续播、移动端手势、长按快进、右键菜单和播放器诊断报告；播放异常时自动尝试切换备用线路，设置页同步展示完整播放器和 HLS 内核版本，继续保持复制链接为包含域名的完整链接，提交后直接同步到远程仓库。
2026-07-08 13:10 【优化】升级版本到 v2.7.1，新增页面悬浮视频按钮，获取到播放链接后可一键暂停网页原生 video/audio 并调用插件 ArtPlayer 浏览器全屏或沉浸全屏播放；自动检测到新版本时直接弹出真实更新弹窗，展示版本号、构建号、发布时间、检测来源和更新内容，并提供「下载」按钮；设置页已检测到远程信息后按钮从「检测并下载」改为「下载」，同步更新版本清单、构建产物和 README。
2026-07-08 13:31 【优化】升级版本到 v2.7.2，播放器画面内控制改为主控按钮加「更多」多级菜单，默认只保留播放/暂停、后退、前进、更多和全屏，减少按钮遮挡视频画面；「更多」里集中放置倍速、重载、比例、HLS 清晰度、备用线路、画中画、复制、打开、下载和诊断；全屏逻辑改为优先请求插件宿主节点进入浏览器全屏，悬浮视频按钮点击时同步申请宿主全屏，避免只进入网页沉浸层仍露出网站导航栏；同步更新版本清单、构建产物和 README。
2026-07-08 13:57 【优化】升级版本到 v2.7.3，播放器控制层新增播放时自动隐藏和移动、触摸、点击唤醒；「菜单」按线路、显示、工具分组收纳功能，避免所有按钮直接挤在视频画面；进度条支持点击跳转；全屏改为浏览器全屏叠加播放器视口覆盖，并隐藏 ArtPlayer 默认底栏，避免网站导航栏或插件导航栏残留；同步更新版本清单、构建产物和 README。
2026-07-08 14:21 【优化】升级版本到 v2.7.4，播放器菜单新增「声音」分组，支持音量滑块、静音切换和音量记忆；「显示」分组新增快进步长切换，主控按钮、双击、长按、媒体键和键盘方向键都会跟随当前步长；新增空格/K 播放暂停、上下方向键调音量、M 静音、F 全屏等快捷键；全屏模式新增无框铺满样式，清理普通卡片阴影、圆角和 ArtPlayer 内层尺寸限制，避免全屏后画面里仍像套着一个视频框；同步更新版本清单、构建产物和 README。
2026-07-08 14:47 【修复】升级版本到 v2.7.5，重构全屏视频填充策略，默认按原比例尽可能完整显示视频，避免全屏后画面被裁剪或强行拉伸；播放器外层、ArtPlayer 内层和 video 节点都会铺满全屏宿主，避免全屏后仍出现内层视频框；显示菜单新增「原比例」「裁满」「铺满」三种填充模式，声音菜单新增亮度滑块和本地记忆，诊断报告同步记录填充模式与亮度；同步更新版本清单、构建产物和 README。
2026-07-08 15:18 【修复】升级版本到 v2.7.6，修复播放器真实全屏层级问题，浏览器全屏优先请求播放器壳层；宿主全屏兜底时自动隐藏插件面板外壳、侧栏、标题栏、底部导航和其它播放页卡片，并把播放器外壳、ArtPlayer 容器和 video 节点锁定到完整视口，避免全屏后仍出现圆角、内层视频框、面板宽度限制或父容器裁剪；同步更新版本清单、构建产物和 README。
2026-07-08 15:29 【优化】升级版本到 v2.7.7，新增播放器全屏体检能力，全屏时会显示全屏来源、视口覆盖率和容器贴合状态，播放器诊断报告同步写入全屏容器尺寸、当前视口、覆盖率和异常原因，方便继续排查黑边、右上角圆角、内层视频框、父容器裁剪或宿主全屏模式失效问题；同步更新版本清单、构建产物和 README。
2026-07-08 19:38 【新增】升级版本到 v2.9.0，全面重构视频播放内核，新增 XGPlayer 3.0.23（西瓜播放器）作为备用播放引擎，同时保留 ArtPlayer 5.4.0 作为默认引擎。建立统一播放器引擎抽象层（engines/types.ts），ArtplayerEngine 和 XGPlayerEngine 均实现统一接口，通过 PlayerEngineFactory 工厂动态创建和切换引擎。usePlayer 替换为 useEnhancedPlayer，useFullscreen 替换为 useEnhancedFullscreen，新增画中画支持、移动端横屏自动全屏、全屏进入/退出过渡动画。PlayerControls 新增画中画按钮、引擎切换标签、沉浸模式按钮；PlayerSettingsMenu 新增「引擎」设置页，支持在面板内一键切换 Artplayer / 西瓜播放器；PlayerProgressBar 重构拖拽逻辑，悬停时进度条放大更明显，拖拽手柄始终可见，时间气泡跟随鼠标；index.css 新增全屏进入/退出过渡动画（txzz-fullscreen-in / txzz-fullscreen-out），新增播放器加载 Spinner 样式，覆写 XGPlayer 和 Artplayer 内置控制栏避免与自定义控制栏冲突。
2026-07-08 21:39 【修复】升级版本到 v2.9.4，基于测试版 Chromium 真实播放详情页复测，修复横屏视频全屏后暂停、快退、快进、菜单和退出按钮被整体旋转成左侧竖排的问题；全屏方向改为请求浏览器系统横屏，浏览器限制方向锁定时仍保持控件正常横向排布，视频默认按原比例居中完整显示；同步强化全屏舞台贴合视口和圆角清零，避免全屏后仍像套着视频框。
2026-07-08 21:50 【修复】升级版本到 v2.9.5，根据手机 Kiwi Browser 横屏截图继续修复全屏播放体验：视频层改为贴合播放器容器自身 100% 宽高，避免移动浏览器横屏时 `100vw` 与真实全屏区域不一致造成左右黑边不均；横屏小高度设备新增紧凑控制层，压低进度条、按钮和底部面板占用，减少遮挡视频主体；广告清理系统升级到 v2，新增开屏倒计时广告、进入按钮、跳过按钮、全屏广告图片和点击后连环弹窗清理。
2026-07-08 22:12 【修复】升级版本到 v2.9.6，根据手机 Kiwi Browser 全屏截图继续修复横屏视觉不居中问题：全屏宿主、播放器壳层、方向舞台和视频内核统一优先使用实时可见视口变量，减少 Android 横屏下 `100vw` 与真实全屏区域不一致造成的偏移；修复悬浮视频按钮先让插件宿主进入浏览器全屏后，播放器页因全屏模式类时序丢失仍按普通面板尺寸显示的问题；原比例完整显示模式新增手机横屏视觉居中校准，会结合 visualViewport 偏移、屏幕长边差值和安全区估算系统保留区，自动微调视频内容位置；工具菜单「居中」改为重新测量全屏安全区，诊断报告新增手机视觉居中状态。
2026-07-08 23:53 【修复】升级版本到 v3.0.1，修复 node_modules 半损坏导致构建失败的问题：恢复缺失的 `node_modules/.bin/vite.cmd` 和 `node_modules/hls.js/dist/hls.mjs` 后构建恢复正常；新增 `scripts/check-deps.mjs` 依赖完整性自检，`npm run build` 和 `npm run check` 前会先检查 Vite、hls.js、ArtPlayer 关键入口文件；新增 `npm run deps:repair` 自动修复命令，可移除损坏依赖目录并重新执行 `npm install`，同步更新 README 和远程更新清单。
2026-07-09 00:09 【优化】升级版本到 v3.0.2，优化播放器画面点击交互：单击视频画面显示悬浮 UI，再次单击隐藏顶部状态条和底部控制面板；鼠标移动和触摸不会再打断用户主动隐藏状态，双击左右快退快进、中间全屏或播放暂停、长按快退快进、进度条拖动和菜单按钮保持原逻辑，同步更新 README、远程更新清单和构建产物。
2026-07-09 15:30 【优化】升级版本到 v3.0.3，优化插件界面质感与全屏播放体验：全屏控制层改为贴底渐变沉浸样式，新增音量/亮度/快进快退手势 HUD 与控制层锁屏防误触，进度手柄常显，诊断徽标短时自动收起；悬浮球、视频悬浮按钮和控制台面板同步提升阴影描边与移动端安全区适配，同步更新 README、远程更新清单和构建产物。
2026-07-09 21:10 【优化】升级版本到 v3.0.4，继续优化插件糖果风外壳与全屏播放体验：悬浮球、视频悬浮按钮和流程提示条适配刘海与底部安全区；控制台面板阴影、毛玻璃与移动端底栏交互更清晰；全屏采用贴底渐变控制层，进度条加粗、手势 HUD 统一反馈；新增控制层锁屏、顶部返回退出、Esc 先解锁、L 键锁屏；同步构建 dist-ui、更新 update.json 与版本号。
2026-07-09 21:20 【优化】升级版本到 v3.0.5，全面重构播放器交互与按钮体系：抽出 `PlayerChrome` 模块化控制组件；主控栏改为左右分区（播放/快退/快进 + 音量/倍速/菜单/全屏）；音量与倍速支持主栏快开面板；新增清晰度/填充/步长快捷条；二级菜单重做线路/画面/声音/工具，倍速/步长/清晰度改为芯片点选；触摸新增横向滑动快进快退，保留竖滑音量亮度、双击三区、长按倍速与锁屏；同步构建产物与远程更新清单。
2026-07-09 21:45 【修复】升级版本到 v3.0.6，修复点击播放后插件面板消失且无法正常使用嵌入播放器的问题：关闭 ArtPlayer 内置单击播放/双击全屏，视频层点击交给自定义控制层；只有用户主动点全屏才进入沉浸模式，全屏失败不再误藏面板；打开面板和点播放时清理残留全屏宿主类，恢复面板内正常播放。
2026-07-09 22:00 【修复】升级版本到 v3.0.7，修复点击底部「播放」导航后面板和悬浮球一起消失：重构时误删 lucide 的 Layers 图标导入，播放页渲染崩溃拖垮整棵 React 树；已补回导入，并增加页面级错误边界，单页异常不再让悬浮球消失。
2026-07-09 22:15 【修复】升级版本到 v3.0.8，恢复视频画面手势操作：新增手势承接层覆盖视频画面，单击显示/隐藏悬浮控制按钮，中间双击暂停/播放，左右双击快进快退，触摸滑动调节音量/亮度/进度；ArtPlayer 内核继续不接收点击，避免抢手势。
2026-07-09 22:40 【新增】升级版本到 v3.0.9，落地完整专业手势系统 `PlayerGestureSystem`：单击显隐控制、三区双击（含同侧连点累计秒数）、长按左快退/右 3x 倍速、横滑进度、左右竖滑亮度音量、滚轮音量、Shift+滚轮亮度；双击区域闪烁与侧边音量/亮度竖条 HUD；锁屏下仍可双击左右跳进度。
2026-07-09 23:05 【优化】升级版本到 v3.1.0，强化浏览器沉浸全屏：优先对 light DOM 插件宿主申请 Fullscreen API 并使用 navigationUI:hide，失败再尝试页面根节点、video、壳层；系统全屏成功后样式强制铺满并隐藏面板壳；仅 API 被拒时才铺满兜底，更接近网站原生视频全屏。
2026-07-09 23:25 【修复】升级版本到 v3.1.1，根据 Kiwi 横屏截图修复控制栏变形：去掉中间状态空白区，横屏矮屏紧凑贴底控制层，隐藏快捷条，按钮左右贴边，进度条文案简化。
2026-07-09 23:45 【优化】升级版本到 v3.1.2，全屏改为网站同款 Fullscreen API（playerRoot.requestFullscreen + navigationUI:hide，兼容宿主/video/iOS），视频 object-fit:contain 自适应铺满容器。
2026-07-09 23:55 【修复】升级版本到 v3.1.3，修复全屏后视频黑屏：去掉错误 viewport 像素尺寸，全屏强制 100% 铺满，进入全屏后多帧校正 video 绘制，Android 不再优先 video 单独全屏。
2026-07-10 00:10 【修复】升级版本到 v3.1.4，修复有声音无画面：Android/Kiwi 禁止对 video 使用 CSS filter:brightness，亮度改为独立遮罩层，起播强制 filter:none。
2026-07-10 00:25 【修复】升级版本到 v3.1.5，修复点缩小无法回到插件面板：退出全屏时清理强制内联样式与宿主全屏类，系统全屏结束自动回面板播放页，不再卡在竖排假全屏。
2026-07-10 00:40 【修复】升级版本到 v3.1.6，修复全屏黑屏只有声音：全屏 CSS 误给手势层 background:#000 且层级高于 video，盖住画面；退出后规则失效故又能看见。现全屏手势层强制透明。
2026-07-09 22:25 【修复】升级版本到 v3.1.7，修复全屏仍只有声音无画面：取消播放器内部层层 position:fixed 黑底叠层，含 video 的容器改为透明 absolute；强制隐藏 ArtPlayer 海报/遮罩；进入全屏后多帧强制校正尺寸并 kickVideoPaint 重绘 Android 硬件解码合成层。
2026-07-09 22:35 【修复】升级版本到 v3.1.8，修复右侧悬浮视频按钮点击后不能正常全屏播放：新增悬浮全屏意图标记，避免播放页挂载时误清宿主全屏样式；自动全屏改为立刻沉浸并等待播放器就绪后衔接；系统全屏失败时保留 CSS 沉浸兜底。
2026-07-09 22:45 【优化】升级版本到 v3.1.9，缩小中央手势操作提示块（紧凑横条/胶囊，减少遮挡画面）；悬浮全屏对横屏片源强制请求系统横屏并在锁失败时用 CSS 横屏舞台兜底。
2026-07-09 23:05 【删除】升级版本到 v3.2.0，移除获取链接后右侧悬浮视频图标及一键全屏播放功能；新增进入网站视频详情页时阻止自动播放、默认暂停，用户点击播放器后可正常播放。
2026-07-09 23:20 【优化】升级版本到 v3.2.1，广告清理系统升级到 v3：重点强化首次进入全屏弹窗与右上角倒计时识别，首屏 rAF/密集扫/CSS 预隐藏整层移除，设置页新增开屏层与倒计时命中统计。
2026-07-09 23:40 【优化】升级版本到 v3.2.2，基于 Playwright 实测 txh068.com 开屏 DOM（.my-swipe.ad-splash.van-swipe + 右上角数字倒计时）做永久硬杀：主世界循环 remove、Mutation 插入即删、拦截广告 window.open。
2026-07-09 23:55 【修复】升级版本到 v3.2.3，广告清理改为严格模式：删除全部猜测规则，仅清理实测到的 .ad-splash 开屏层，修复过度清理导致无法进入网站的问题。
2026-07-10 00:10 【修复】升级版本到 v3.2.4，紧急恢复 content.js 被误删的核心函数与初始化链路，广告清理继续仅针对实测 .ad-splash，插件主体功能恢复可用。
2026-07-10 01:00 【优化】升级版本到 v3.3.0，完全重构升级系统到 v4：新增 `ui-src/app/update/` 模块（helpers 状态机、UpdateCenter 升级中心、UpdateModal 专业弹窗）；App 顶部状态徽标与自动弹窗接入统一视图模型；设置页移除旧破碎更新弹窗，仅保留升级中心；后台 schema v4、changelog 规范化、多候选下载与忽略提醒；同步 manifest、package、update.json、构建号与构建产物。
2026-07-10 01:15 【优化】升级版本到 v3.3.1，缩小播放器主控与多级菜单功能图标：全屏主控改为紧凑 md 尺寸；菜单芯片与工具宫格图标约 10–12px，工具区改为上图标下文字四列布局；CSS 强制限制 more-sheet 内 svg 尺寸，减少遮挡画面。
2026-07-10 02:00 【优化】升级版本到 v3.4.0，完全重构插件面板 UI：新增 `ui-src/app/components/ui/primitives.tsx` 统一设计系统；总览/账号/下载/设置页按 SectionCard、StatGrid、HeroBanner、ModalSheet 等组件重做布局；App 外壳侧栏/顶栏/底栏与面板动效升级；播放页外围卡片视觉对齐；同步版本号、update.json、构建产物与 README。
2026-07-10 02:15 【修复】升级版本到 v3.4.1，修复总览「快捷操作」仅同步账号显示背景、其余按钮白底不可见（Tailwind 动态 class 未扫入）；下载页移除顶部重复 StatGrid，仅保留分段筛选数量。
2026-07-10 02:30 【修复】升级版本到 v3.4.2，快捷操作改内联渐变+CSS 强制白字彻底修白底；更新检测增加 jsDelivr 多镜像轮询，结果展示本地/远程比对说明，降低 raw.githubusercontent 失败导致“检测不到新版本”。
2026-07-10 03:00 【优化】升级版本到 v3.4.3，重构播放页布局：紧凑信息头与主操作；播放器优先；下方「资源/下载/记录」分段切换，合并体检分片线路；记录区筛选排序与空状态统一；播放器内核与面板底栏导航不变。
2026-07-10 03:15 【修复】升级版本到 v3.4.4，播放页顶部信息头与按钮改内联底色；播放记录按 movieId 合并去重，备用线路并入同一条，不再重复显示两个视频记录。
2026-07-10 04:00 【优化】升级版本到 v3.5.0，主备线路按分片/时长/延迟综合打分推荐；下载支持选线（自动/主线/备用），实时体积百分比进度与下载速度，下载页同步展示。
2026-07-10 04:15 【修复】升级版本到 v3.5.1，广告清理补杀开屏倒计时结束后的右上角进入/跳过/数字残留按钮（可能挂在 body，不在 .ad-splash 内），并加强首屏补扫与点击拦截。
2026-07-10 04:30 【修复】升级版本到 v3.5.2，修复 background.js 中 `??` 与 `||` 混用导致的语法错误，Service Worker 无法启动，同步账号与检测更新全部失效。
2026-07-10 04:45 【优化】升级版本到 v3.5.3，线路探测改为后台延迟串行静默执行，播放中锁定当前源避免探测改分重载；体检/诊断/记录报告仅面板内查看，不再写入剪贴板。
2026-07-10 05:05 【修复】升级版本到 v3.5.4，升级系统 v5：更新检测改为多源并发探测，按 version+build 取最新清单，修复 jsDelivr 强缓存导致「本地 3.5.3 / 云端却显示 3.5.1～3.5.2、检测不到更新」；设置页展示探测摘要；同步 manifest、package、update.json、构建号与构建产物。
2026-07-10 05:20 【新增】升级版本到 v3.5.5，正式分发改为 CRX：新增 `scripts/pack-crx.mjs` 与 `npm run pack` / `npm run release`；产物 `releases/tangxin-zhizhe-latest.crx`；检查更新下载优先拉取 CRX（多镜像），保存名为 `.crx`；固定私钥签名保证扩展 ID 稳定；同步文档安装与发版说明。
2026-07-10 08:26 【优化】升级版本到 v3.6.0，全面升级云端连接、账号池与控制台体验：新增后台 Bearer 访问密钥、密钥脱敏状态、安全体检、账号搜索、可靠编辑、删除二次确认、面板内操作反馈、云端状态徽标、键盘焦点、弹窗焦点循环、悬浮球位置限制与持久化；提升文字对比度、减少动态效果适配和 320/390 宽移动端布局；依赖升级到 Vite 8.1.4、Tailwind CSS 4.3.2、TypeScript 7.0.2，并补齐严格类型检查。
2026-07-10 08:26 【修复】修复账号卡片“编辑”仍调用旧虚拟 DOM 导致新版 React 弹窗无法打开、编辑留空凭据无法保留、token 账号留空编辑时 deviceId 被意外清除、deviceId 被发送到页面状态、中文用户名可能生成重复编号、保存云端配置失败仍显示成功等问题；新增本地删除确认，防止误操作。
2026-07-10 08:26 【修复】修复 VIP 账号已经返回主线路或备用线路时仍按 `has_buy/layer_type/money` 标记触发金币购买的问题；现在可播放链接优先级最高，只有确实没有链接时才进入购买流程。
2026-07-10 09:27 【优化】取消云端访问密钥手工填写，插件后台与 Worker 改为使用一致的内置访问密钥；用户填写服务地址即可直接体检、同步和播放，覆盖升级时会自动清理旧版手填密钥字段。
2026-07-10 09:27 【修复】修复“账号状态记录：购买/解锁/已观察”被高频网络观察反复写入而长期显示的问题；被动观察改为只保留诊断记录，历史提示不再补弹，正常操作提示约 3.2 秒后自动关闭；面板在提示显示期间关闭时会立即清空提示，避免下次打开恢复为常驻状态，并修复窄屏换行、关闭按钮和底部导航遮挡。
2026-07-10 09:27 【修复】修复真实网站缩小根字号后插件间距、触控按钮和响应式断点同步缩小的问题；新增独立 4px 间距基准、固定像素断点和手机顶部显隐保护，使用独立 MCP/CDP 测试 Chromium 验证 320、390 与桌面五页无横向溢出。
2026-07-10 09:27 【修复】扩展 VIP 播放线路判定，无扩展名签名地址、相对备用地址和嵌套签名线路同样优先直接播放；即使主字段是占位值，只要其他字段存在真实线路也绝不购买，只有全部候选字段均为空或占位值时才允许进入金币购买流程。
2026-07-10 10:10 【修复】完成 v3.6.0 最终回归补丁：面板关闭时立即清空正在显示的操作提示，修复下次打开恢复为常驻提示；补齐主字段占位但其他字段已有签名或嵌套线路时的免购买保护；token 账号留空编辑完整保留 deviceId 与 userToken，页面状态不再获得 deviceId 明文，并为中文账号生成稳定编号。
2026-07-10 10:10 【优化】最终构建号更新为 `2026-07-10-1010`，同步更新升级清单、生产构建和 CRX 发布包说明。
2026-07-10 13:03 【优化】升级版本到 v3.7.0，统一专业工作台设计令牌与组件规范，全面重构总览、账号池、播放、下载、设置和升级中心；桌面端使用深色侧栏与宽屏双栏播放保障，移动端使用安全区底栏，并完成 320、390、640、1024、1536 像素布局适配。
2026-07-10 13:03 【新增】新增播放保障信息区、设置分类导航、下载批量操作菜单、失败任务节流重试、可访问弹层焦点循环、Esc 关闭与关闭后焦点恢复。
2026-07-10 13:03 【修复】修复面板双滚动、Shadow DOM 动态渐变和设计令牌失效、流程提示常驻、缓存选择假复选框、清空操作重复确认、无线路误标推荐和空详情虚假体检分等问题。
2026-07-10 13:03 【修复】在真实网站验证金币视频 35602：详情返回主备播放链接后使用 `direct_full_detail` 直接播放，未携带购买策略、未标记金币购买且未发出购买请求；同步保持 VIP 已有链接时绝不购买的服务端保护。
2026-07-10 14:30 【优化】完成 v3.7.0 播放器悬浮控制与全屏系统重构：将 913 行控制单体拆分为主控栏、四分类菜单、状态覆盖层和手势模块；高频操作固定、低频能力按播放/画面/音频/工具收纳，新增可聚焦进度条、ARIA、菜单 Esc 优先级、全屏覆盖式滚动菜单和锁屏独立解锁入口。
2026-07-10 14:30 【修复】修复播放器缓冲期间点击暂停反而继续播放、Shadow DOM 进度条方向键被全局热键重复执行、音量与亮度键盘反馈缺失、重复全屏滚轮链路、退出全屏残留强制样式，以及锁屏后画面仍响应手势的问题；新增 `docs/player-system.md` 功能矩阵和全屏状态顺序。
2026-07-10 14:30 【优化】将升级更新系统重构为 v6：新增 15 分钟成功缓存、手动实时绕过、多标签检测任务去重、可信 HTTPS 域限制、远程清单格式校验、CRX2/CRX3/ZIP 文件头校验、重定向校验和失效镜像自动切换；升级中心与弹层共用真实阶段进度，展示缓存年龄、镜像健康、逐源错误、包校验和浏览器下载编号，并明确下载后仍需手动安装。
2026-07-10 14:30 【修复】修复手动检查误复用自动缓存任务、忽略更新重复写入且刷新后仍反复弹出、固定计时器提前结束忙碌状态、已提交下载误表述为完成安装、CRX2 头长度解析不准确、下载重定向未严格限制，以及同版本低优先级镜像被误计为旧版本源的问题；构建号更新为 `2026-07-10-1430`。
2026-07-10 14:30 【新增】新增 `scripts/check-release.mjs` 发布一致性门禁与 `docs/update-system.md` 状态文档；`npm run check` 校验源码版本一致性，`npm run release` 进一步校验 CRX 文件头、大小、SHA-256、版本文件与 latest 文件一致性以及固定扩展 ID。
2026-07-10 14:30 【优化】单独重构检测更新弹窗：桌面端使用更宽但克制的版本对比弹层，移动端保持底部安全区布局；统一展示真实四阶段进度、检测来源、缓存状态、镜像健康、CRX 校验、更新内容、下载编号和手动安装边界，320 像素下使用单行短按钮与紧凑阶段名，并取消更新列表的嵌套滚动；关闭弹窗不再等同于忽略更新，只有明确点击“本次更新不再自动提醒”才持久化忽略。
2026-07-10 16:50 【优化】完成 v3.7.0 播放器最终可靠性收口：历史记录预览使用记录自己的 movieId、主备线路与当前线路，紧凑横屏保留锁屏入口；全屏状态加入 generation 与统一计时任务管理，安全区偏移实际作用于视频居中，快速进退全屏时旧回调不再覆盖新状态。
2026-07-10 16:50 【修复】修复历史预览切线、下载、复制和打开操作误用最新视频上下文，HLS 恢复后仍保留致命错误计数和错误遮罩，右键菜单被手势层阻断，播放器菜单缺少焦点循环与键盘方向导航，以及 Ctrl+F、Alt+方向键被播放热键误拦截的问题。
2026-07-10 16:50 【优化】将升级系统加固为 v7：远程清单升级为 schema 3 RSA 签名格式，内置正式公钥并固定完整仓库路径；多源只接受有效签名，保存最高已验证版本防回退，自动缓存、实时检测和下载前清单采用独立任务契约，升级状态通过串行队列读改写。
2026-07-10 16:50 【修复】安装包改为单次完整获取并校验同一份字节的大小、SHA-256、CRX3 内嵌公钥、正式扩展 ID、ZIP 起点与包自身 RSA 签名，再从已验证内存数据提交浏览器下载；修复 Range 探测与实际下载之间的替换竞态、并发检测覆盖忽略 ID、状态保存失败造成重复下载，以及私钥缺失时静默生成新身份的问题。
2026-07-10 16:50 【优化】重构升级中心和检测更新弹窗：所有入口精准进入“设置 → 版本升级”，自动弹窗避开全屏、其他弹层和输入状态；底层弹窗使用 inert 与 aria-hidden，顶层独占 Esc 和焦点；错误检测不再开放下载，忽略和复制失败均明确反馈，320～1536 像素统一使用签名清单、版本比对、完整包验证和提交下载四阶段。
2026-07-10 16:50 【新增】新增公开签名元数据与发布共享配置；发布门禁会验证 update.json 签名、CRX3 自身签名、固定扩展 ID、ZIP 中央目录、CRC32 和大小写冲突，并把 CRX 内全部运行时文件与当前工作区逐字节核对；最终构建号更新为 `2026-07-10-1650`。
2026-07-10 17:12 【修复】真实 Playwright Chromium 加载检查发现 Shadow DOM 后置 ArtPlayer 全屏样式仍把视频位置固定为中心，现统一改为消费安全区居中变量，避免覆盖全屏左右安全区校正；同步将最终构建号更新为 `2026-07-10-1712` 并重新生成生产界面、签名清单和 CRX3 发布包。
2026-07-10 20:50 【修复】升级版本到 v3.7.1：操作提示改为消费 visualViewport 的真实中心与宽度并使用传统 transform 居中，修复 Android Edge 中提示从屏幕中心向右溢出、关闭按钮和文案被截断的问题；播放器按输入来源分流 contextmenu，触摸与手写笔长按只执行连续快退或临时倍速且不再弹出链接菜单，桌面鼠标右键菜单保持可用；同步补充触摸回归说明、构建产物、签名清单与 CRX3 发布包。
[2026-07-26 22:54] 【重构】源码升级到 v4.0.0（构建 `2026-07-26-2254`）：以「糖果手帐」主题重做悬浮伙伴、桌面侧栏、移动底栏、页面信息头、卡片、统计、弹层、表单与状态反馈；总览改为今日运行清单、正在进行、快捷动作和小日记，账号池、播放、下载与设置分别重命名为账号小屋、放映室、下载收纳篮和照料中心，同时保持原事件动作和 Worker 接口兼容。
[2026-07-26 22:54] 【重构】拆分 React 业务结构：新增 `domain/` 账号与下载规则、`model/` 导航和跨页状态视图模型、`hooks/` 偏好持久化以及 `components/layout/` 工作台壳层，`App.tsx` 体积约减半；桌面与移动五页交互检查、TypeScript 与生产构建通过。
[2026-07-26 23:04] 【发布】原 3.7.1 正式私钥确认永久丢失后，按用户要求生成并启用新签名身份；4.0.0 正式扩展 ID 调整为 `ddefadnhgebdclpkabeobjidjllkdkhm`，同步固定公钥、运行时信任锚、签名清单和 CRX3。由于 Chrome 扩展身份取决于公钥，旧 ID 用户必须移除旧版并手动安装 4.0.0，无法原位覆盖升级。
[2026-07-28 01:18] 【修复】升级到 `5.1.1`（构建 `2026-07-28-0118`）：Vlog 当前卡片改由主世界 `page_hook` 通过 `pageEpoch + contextRevision` 向隔离世界发布权威快照，修复隔离世界读不到 Vue `__vue__` 导致正常当前视频被误报为预加载；旧代次响应仍会安全拒绝且不再弹出误导性错误。
[2026-07-28 01:18] 【修复】广告清理新增全站顶部 `#__layout > .app-container > .control` 全宽「糖心Vlog / 倒计时 / 进入」蒙层的结构、几何与文案三重校验，不影响播放器内同名控件。实测 Vlog 12997 完整线路 599.9 秒成功回填，快速 6629 → 7024 换片时旧响应被拒绝且当前 7024 保持 182.97 秒完整线路。
[2026-07-30 02:40] 【修复】升级到 `5.4.1`（构建 `2026-07-30-0240`）：补齐目标站 `.bnc` 海报 AES-128-ECB/PKCS7 解密、严格 URL/大小/魔数校验、Hero/海报墙/详情统一懒加载与有限缓存；搜索可与热门、权益和横竖屏组合，目录缓存可跨 Service Worker 重启恢复，旧片单刷新失败时显示可重试提示且不清空已有内容。
[2026-07-30 02:55] 【修复】升级到 `5.4.2`（构建 `2026-07-30-0255`）：海报解密改为单次 Web Crypto CBC + 线性异或还原 ECB，并禁止网络重定向，补齐整数/八进制 IPv4 与映射地址防护；目录请求纯核心固定为 block/search 两个只读端点。`npm run pack` 写出产物后立即执行完整 CRX3、签名、哈希、ZIP 和逐文件一致性门禁。
[2026-07-30 21:36] 【修复】扩展 `5.6.0` 修复下载任务不存在时被空对象误判为 `queued`、随后读取 `mode` 报错的问题；旧任务会按媒体地址补齐下载模式，暂停、继续、取消严格遵守阶段状态机，完成或待保存任务不再退回运行态。独立影院同时补齐外部 Hash、浏览器前进与后退的路由同步，并在真实安装的 Chrome Dev 中通过桌面、手机、横屏和 `320 / 640 / 1024 / 1536` 宽度验收。
