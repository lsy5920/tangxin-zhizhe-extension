# 升级更新系统 v8

升级系统负责发现版本、验证发布身份、下载并校验安装包、把成品写入 OPFS，以及通过扩展来源安全保存页交给用户。扩展受浏览器安全边界限制，不能静默安装 CRX，因此系统会明确区分“检测完成”“安全保存页已打开”“文件已保存”和“用户已手动安装”。

## 用户状态矩阵

| 阶段 | 机器状态 | 用户反馈 | 恢复路径 |
| --- | --- | --- | --- |
| 未检测 | `idle` | 只显示本地版本和“检查更新” | 用户发起实时检测 |
| 检测中 | `checking` | 显示正在读取并验证多源签名清单 | 等待、超时后重试 |
| 缓存命中 | `cached` | 显示上次成功结果和缓存年龄 | 手动检测强制绕过缓存 |
| 已是最新 | `success/latest` | 显示本地、远程版本与构建 | 可重新检查或重新下载同版本 |
| 有新版本 | `success/available` | 显示版本差异、日志和下载入口 | 可稍后处理或明确忽略此版本 |
| 检测失败 | `error` | 显示签名、网络或清单错误 | 重试；不提供未经校验的主下载动作 |
| 完整校验中 | `validating` | 逐镜像下载完整 CRX3，并校验大小、哈希、身份和包签名 | 失败自动切换下一个固定镜像 |
| 等待安全保存 | `saving` | CRX 已写入 OPFS，显示一次性保存票状态 | 在新标签点击保存；关闭后可重新生成票据 |
| 已提交下载 | `submitted` | 安全保存页确认文件已写盘或用户已确认下载文件可见 | 到扩展管理页手动安装 |
| 下载失败 | `failed` | 显示每个镜像的验证或提交错误 | 修复网络后重试 |

## 信任链

1. `update.json` 使用 schema 3。除 `signature` 本身外，版本、构建、更新日志、扩展 ID、安装包大小、SHA-256 和全部候选地址都会被规范化后签名。
2. 后台内置正式 RSA 公钥，只接受 `RSASSA-PKCS1-v1_5-SHA-256` 和固定公钥指纹。任意镜像可以缓存或转发清单，但无法伪造更高版本。
3. 清单和 CRX 地址都按完整的 owner、repo、branch、path 白名单匹配，不再仅凭“域名后缀看起来可信”放行。
4. 多个已通过签名的清单按 `version → build → 来源优先级` 选择最新结果，并保存已验证最高版本，拒绝后续签名清单回退。
5. 下载前重新取得实时签名清单。每个候选镜像的完整字节依次验证：

   - 文件长度与签名清单的 `packageSize` 完全一致；
   - SHA-256 与 `packageSha256` 完全一致；
   - CRX 魔数、CRX3 版本、签名头长度和头后的 ZIP 魔数正确；
   - CRX3 内嵌公钥、`crx_id` 与正式扩展 ID `ddefadnhgebdclpkabeobjidjllkdkhm` 一致；
   - CRX3 自身 RSA 签名有效。

6. 校验成功后，后台只把固定 URL、已验证大小和 SHA-256 交给 Offscreen；Offscreen 重新获取文件并复核相同哈希后写入 OPFS。即使镜像在两次请求之间变化，哈希不匹配也会拒绝，不存在未验证字节落入保存页的 TOCTOU 窗口。
7. 后台生成 10 分钟一次性令牌并打开扩展来源 `save.html`。令牌通过串行队列领取，同一时刻只能绑定一个仍打开的标签；原标签可刷新或重试，原标签关闭后新标签可安全接管未核销令牌。保存页从 OPFS 读取成品，再次验证大小、CRX3 头和 SHA-256。
8. 支持 File System Access API 时，文件流实际关闭成功后才核销令牌；不支持该 API 的 Android Chromium 使用用户点击的 Blob 下载，不能从 `anchor.click()` 推断落盘，因此保留令牌、允许重复提交，并在用户确认看到文件后才更新为 `submitted`。
9. 整个 CRX 不经过 runtime 响应、Base64 或单条大消息传输，也不依赖 `chrome.downloads.download`。
10. 没有通过签名的清单时拒绝下载；远程版本低于本地版本或构建时拒绝降级。

## 并发与持久化

- 自动缓存检测、用户实时检测和下载前“必须取得签名清单”使用不同任务契约，互不错误复用。
- 相同契约在 Service Worker 内去重，多个标签页不会同时制造重复请求。
- 升级状态统一通过串行写队列读改写，检测完成不会覆盖刚写入的 `dismissedId` 或下载状态。
- 忽略操作必须由用户明确触发，并按远程更新 ID 持久化；写入失败会反馈失败，不能伪装成功。
- 令牌领取、核销和升级状态写入均串行执行；业务状态成功持久化后才删除令牌，状态写入失败时原标签仍可重试确认。
- 安全保存页关闭后，可重新打开同一未核销令牌；后台确认旧领取标签已经关闭后才允许新标签接管。升级中心也可重新验证并生成新令牌，所有令牌仍受十分钟过期限制。
- 自动检测仅缓存成功结果 15 分钟；失败结果不缓存，手动检测永远实时绕过。
- 成功缓存必须同时匹配当前安装的 `version / build` 本地指纹；覆盖安装后即使旧缓存仍写着 `updateAvailable: true`，也会绕过缓存并重新比较，界面还会使用当前打包常量进行第二次复核。

## 签名清单关键字段

| 字段 | 含义 |
| --- | --- |
| `schema` | 固定为 `3` |
| `version` / `build` | 语义版本与精确构建号 |
| `extensionId` | 固定正式扩展 ID |
| `packageFormat` | 固定为 `crx` |
| `packageSize` | 完整 CRX 字节数 |
| `packageSha256` | 完整 CRX 的小写 SHA-256 |
| `downloadUrl` / `downloadCandidates` | 固定正式仓库路径的镜像列表 |
| `signature.algorithm` | `RSASSA-PKCS1-v1_5-SHA-256` |
| `signature.keyId` | 固定公钥 SHA-256 标识 |
| `signature.value` | 规范化清单正文的 Base64 RSA 签名 |

公开公钥元数据位于 `releases/signing-public-key.json`。私钥只允许存在于本机被 Git 忽略的 `keys/txzz-extension.pem`；私钥缺失时打包命令会直接失败，不会自动生成另一个扩展身份。

## 源码版本与已签名发布

开发源码允许先提升 `manifest.json`、`package.json`、前端常量和后台本地构建号，而 `update.json` 与 `releases/` 继续保留上一个已签名版本。`npm run check` 的源码门禁会分别校验两组数据：源码内部必须一致，现有签名清单自身也必须完整；它不会把未签名源码伪装成已经发布。

只有 `npm run release` 会要求源码版本、`update.json`、`latest.json` 和 CRX 完全一致，并使用固定私钥重新签名。当前目标源码为 `5.8.0 / 2026-07-31-1755`，继续使用 4.0.0 启用的固定私钥，扩展 ID 保持为 `ddefadnhgebdclpkabeobjidjllkdkhm`。

## 4.0.0 签名身份轮换

原 3.7.1 私钥已永久丢失，无法重新获得旧扩展 ID 的签名能力。经明确确认，4.0.0 生成并启用了新的 RSA 私钥；旧 ID `ghbbddahmhhmjknofkmdkcflbmplcace` 与新 ID `ddefadnhgebdclpkabeobjidjllkdkhm` 是两个不同的 Chrome 扩展身份，因此旧版不能验证新清单，也不能原位覆盖安装。迁移时应先记录服务地址等仅保存在旧扩展本地存储中的设置，再移除旧版并手动安装 4.0.0。云端 Supabase 账号数据不依赖浏览器扩展 ID，但本地偏好需要重新配置。

## 模块边界

- `background.js`：签名清单验证、多源比较、防回退、完整 CRX3 验证、OPFS 保存票、单标签领取和状态串行写入。
- `offscreen_downloader.js`：按固定 URL 重新获取 CRX、复核大小/CRX3/SHA-256 并写入 OPFS；不读取 `chrome.downloads`。
- `save.html / save.css / save.js`：扩展来源安全保存页，负责 OPFS 读取、二次校验、文件写盘、Android Blob 兜底和票据核销。
- `content.js`：发布真实检测/下载阶段，设置消息超时并转发明确的忽略与下载动作；不接收整个 CRX 字节。
- `ui-src/app/update/helpers.ts`：把后台阶段归一为用户状态，不使用固定计时器伪造进度。
- `ui-src/app/update/UpdateProgress.tsx`：升级中心和更新弹层共用的阶段组件。
- `ui-src/app/update/UpdateCenter.tsx`：完整版本、缓存、镜像、验证结果、逐源错误和安装边界。
- `ui-src/app/update/UpdateModal.tsx`：新版本提醒与关键动作；关闭、稍后处理和忽略此版本语义分离。
- `scripts/release-config.mjs`：打包和发布门禁共用的正式身份、公开公钥与运行时文件清单。
- `scripts/crx3-pack.mjs`：使用 Node.js `crypto` / `zlib` 构造确定性 ZIP、CRX3 `signed_header_data` 和 RSA proof；拒绝目录穿越、大小写冲突、符号链接和 ZIP64，不依赖第三方归档库。
- `scripts/pack-crx.mjs`：调用内置 CRX3 打包器和固定私钥，计算大小/哈希并签署外置 `update.json`。
- `scripts/check-release.mjs`：源码模式分别校验开发版本与现有签名发布的一致性；完整模式验证两者已对齐，再验证清单签名、CRX3 包签名、ZIP 中央目录/CRC32，并逐字节核对 CRX 内全部运行时文件。
- `cinema.html / cinema.css`：5.6.0 起作为独立影院标签页的正式运行时文件，与 `dist-ui` 资源一同进入发布白名单、CRX ZIP 和逐字节一致性校验。

## 防止哈希自引用

`update.json` 包含 CRX 的哈希，因此不能再打进该 CRX，否则会形成“修改清单改变 CRX、改变 CRX 又必须修改清单”的自引用。当前发布清单和 Manifest 的 `web_accessible_resources` 都已移除 `update.json`；它只作为仓库根目录的外置签名发布元数据。

## 发布与验收

1. 完成源码、界面和文档修改；仓库不再保留本地模拟预览页。
2. 执行类型检查、测试和生产构建，把当前目录作为未打包扩展安装到完整 CDP 权限的 Chrome 开发版，直接验收真实扩展页面和 Service Worker。
3. 执行 `npm run release`；命令会再次做类型检查和生产构建，再用固定私钥打包并签署清单。
4. 完整门禁解析 CRX2/CRX3（正式产物强制 CRX3），校验 CRX 签名与扩展 ID，解压 ZIP 并逐文件比对当前工作区。
5. 推送源码、`update.json`、`releases/latest.json`、公开公钥元数据和两个 CRX 文件。

## 浏览器安装边界

- Kiwi 等支持 CRX 的浏览器可直接打开下载文件安装。
- 桌面 Chromium 可能拦截非商店 CRX；可在开发者模式加载解压目录。
- `submitted` 只表示已把验证后的文件交给浏览器下载，不代表浏览器已下载完成，更不代表新版本已经安装。

[2026-07-27 19:25] 升级系统提升到 v8：CRX 改为 OPFS + 扩展安全保存页交付，移除 runtime Base64 和页面内容脚本大包通道；新增单标签一次性令牌、写盘后核销、Android 显式确认与关闭后重试。
[2026-07-28 09:50] 扩展 5.1.2 将安全保存成功动作明确显示为“保存完成”；OPFS、一次性令牌、CRX3/SHA-256 复核和 Android Blob 兜底语义保持不变。
[2026-07-28 09:50] 发布工具移除 `crx/archiver` 依赖，改为确定性内置 CRX3 打包器；新增 RSA proof、`crx_id`、篡改拒绝、路径穿越和大小写冲突测试，完整发布门禁继续逐文件核对 ZIP 与工作区。
[2026-07-29 18:10] 扩展 5.2.0 新增片库、书签、下载调度、OPFS 管家和账号巡检；更新检测、CRX 保存、签名及哈希校验流程保持不变。
[2026-07-29 19:29] 扩展 5.2.1 修复 Shadow DOM 下 Tailwind 注册属性失效造成的生产样式缺失；发布门禁新增属性基线和内联 CSS 校验，正式身份与 CRX 保存协议不变。
[2026-07-29 20:30] 扩展 5.2.2 将升级状态缓存绑定本地版本/构建指纹，修复同版本误报与 Service Worker 调用未定义 `parseBuildStamp` 导致实时检查失败的问题；删除本地模拟预览页及其门禁，所有界面验收统一改为安装生产扩展后执行。
[2026-07-30 00:14] 扩展 5.3.0 迁移到 Shaka Player 成熟内核并增加独立目标帧预览；更新系统协议不变，正式 CRX 继续使用固定私钥和扩展 ID，新增的 transmux Worker 作为运行时文件参与逐字节包内容门禁。
[2026-07-30 11:22] 扩展 5.5.0 增加独立影院 App、合集续播与影院下载入口；升级系统协议、固定私钥、扩展 ID、OPFS 安全保存和清单签名规则保持不变。
[2026-07-30 21:36] 扩展 5.6.0 将影院迁移到独立 `cinema.html` 标签页，并把 `cinema.html / cinema.css` 纳入运行时白名单、源码门禁和 CRX 逐文件核验；升级系统 v8 协议、正式私钥、固定扩展 ID 与 OPFS 安全保存语义不变。
[2026-07-30 21:36] 扩展 5.6.0 发布前完成真实安装验收：下载规划可在约 177 毫秒内入队并完成 33 段 HLS，暂停、继续、取消及 OPFS 清理通过；独立影院桌面、手机、横屏、边界宽度和浏览器历史路由通过，随后重新执行全量测试、生产构建、CRX3 签名与逐文件一致性门禁。
[2026-07-31 01:45] 扩展 5.7.0 发布继续沿用升级系统 v8、固定私钥与正式扩展 ID；独立影院、播放器、下载规划和生产样式均进入同一 CRX3 逐文件一致性门禁。
[2026-07-31 01:53] 扩展 5.7.1 修复移动端主视觉和矮横屏底栏遮挡；升级清单、CRX3 签名、正式 ID 与逐文件一致性门禁保持不变。

[2026-07-28 22:27] 扩展 5.1.3 仅调整网站覆盖层显示：移除左上角流程气泡和 Vlog 电影票 HUD；更新检测、CRX 保存、签名及哈希校验流程不变。
